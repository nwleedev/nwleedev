# 개인 메모 애플리케이션 요구사항

현재 개발 범위는 별도의 백엔드와 데이터베이스 없이 동작하는 로컬 개인 메모 애플리케이션을 완성하는 것이다. 계정 생성, 로그인, 동기화, 백엔드와 데이터베이스는 초기 범위에서 제외하되 모든 백로그 가운데 가장 먼저 진행할 항목으로 둔다. 애플리케이션은 독립적인 웹 애플리케이션으로 실행할 수 있어야 한다. 라이브러리 패키지 빌드와 포트폴리오 웹 애플리케이션 연결도 초기 범위에서 제외한 백로그다. 이 문서는 요구사항 책임자가 요청한 결과를 정리한 기준선이며, 구현 담당자와 검토자는 현재 범위에서 사용자가 완료할 수 있어야 하는 과업과 완료 증거를 확인한다. 조사에서 도출한 구현 제안은 이 문서가 아니라 `references/` 아래 문서에서 다룬다.

## 의도한 결과

- 사용자는 여러 메모를 만들고 각 메모에 텍스트를 입력할 수 있다.
- 사용자는 다른 애플리케이션에서 복사한 텍스트를 애플리케이션에서 만든 메모에 붙여넣을 수 있다.
- 사용자는 메모 작업 영역이 충분히 넓을 때 메모를 단순 목록이나 그리드에만 배치하지 않고, 공간형 화면에서 각 메모의 위치와 크기를 자유롭게 바꿀 수 있다.
- 사용자는 메모를 만들고 배치하는 화면에서 왼쪽 사이드바 없이 메모 작업 영역을 사용할 수 있다.
- 사용자는 메모 작업 영역의 너비가 작은 환경에서 공간형 화면 대신 일반적인 목록 형태로 메모를 확인할 수 있다.
- 사용자는 넓은 메모 화면에서 별도 편집 단계로 전환하지 않고 항상 메모 본문을 작성하고 고칠 수 있으며, 작은 화면에서는 메모 상세 화면에서 원문을 수정해 저장할 수 있다.
- 사용자는 메모에 URL이나 링크처럼 보이는 문자열을 입력하더라도 다른 텍스트와 같은 방식으로 작성, 편집하고 복사할 수 있다.
- 사용자는 메모 본문을 `Command+클릭`해 메모 원문 전체를 복사할 수 있다.
- 사용자는 여러 메모에서 한 번에 복사하려는 텍스트를 `Command+Option+클릭`해 일괄 복사 목록에 추가하고, 넓은 메모 화면의 보조 패널에서 선택한 텍스트를 확인할 수 있다.
- 사용자는 별도 설정 화면에서 `Command+Option+클릭` 일괄 복사 동작을 켜거나 끌 수 있다.
- 사용자는 각 메모의 짧은 헤더를 드래그해 메모를 이동하고, 헤더의 아이콘 버튼으로 메모를 다른 메모보다 맨 앞이나 맨 뒤에 표시하거나 삭제할 수 있다.
- 사용자는 작은 화면의 제한된 높이 메모 목록에서 메모를 짧게 눌러 상세 화면으로 이동하고, 상세 화면에서 원문을 수정해 저장할 수 있다.
- 사용자는 작은 화면의 메모를 길게 눌러 해당 메모의 원문 전체를 개별 복사할 수 있다.
- 사용자는 작은 화면의 메모 목록 헤더에서 일괄 복사 상태를 시작하고, 상태가 활성화된 동안 메모를 짧게 누른 순서대로 텍스트를 모은 뒤 화면 아래에서 현재 작업을 초기화하거나 다음 단계로 이동할 수 있다.
- 사용자는 작은 화면에서 우측 하단의 항목 수 제어를 눌러 일괄 복사 관리 페이지로 이동하고, 목록을 재정렬한 뒤 하나로 합친 텍스트를 복사할 수 있다.
- 사용자는 넓은 화면에서 `Tab` 또는 메모 헤더 클릭으로 메모 하나를 선택하고, 오른쪽 패널에서 선택한 메모의 좌표와 크기를 바꿀 수 있다.
- 사용자는 메모를 제거한 직후 주 작업 영역의 안내에서 제거를 취소하고 같은 메모를 복원할 수 있다.
- 사용자는 일괄 복사 목록의 순서를 드래그 앤 드롭으로 바꾸고, 특정 텍스트를 목록 밖에 놓아 제거할 수 있다.
- 사용자는 일괄 복사 목록에서 잘못 제거한 텍스트를 실행 취소하고, 실행 취소한 제거를 다시 실행할 수 있다.
- 사용자는 메모가 아닌 빈 캔버스를 드래그해 현재 바라보는 작업 위치를 바꿀 수 있다.
- 사용자는 텍스트의 개별 복사 횟수, 일괄 복사 횟수와 두 횟수의 합계를 별도 화면에서 확인할 수 있다.
- 사용자는 메모 안의 텍스트를 줄바꿈으로 나눈 각 줄이 생김새를 기준으로 정확히 같거나 포함되는지, 문자열 조각이 얼마나 겹치는지를 별도 화면에서 확인할 수 있다.
- 사용자는 생김새가 겹치는 텍스트를 바탕으로 템플릿 텍스트를 제안받을 수 있다.
- 사용자는 텍스트에서 원하는 여러 부분을 플레이스홀더로 표시하고 템플릿으로 만들 수 있다.
- 사용자는 템플릿 텍스트에 필요한 값을 입력해 일회성 텍스트를 만들 수 있다.
- 사용자는 별도의 백엔드와 데이터베이스 없이 동작하는 로컬 모드에서 메모 작성, 편집, 배치, 개별 복사, 일괄 복사, 템플릿 생성과 명시적으로 요청한 줄 단위 텍스트 분석을 사용할 수 있다.
- 사용자는 개인 메모 애플리케이션을 독립적으로 실행해 사용할 수 있다.
- 사용자는 모든 화면에서 개인 메모의 공간 작업 특성에 맞춘 일관된 시각 언어와 조작 상태를 확인할 수 있다.
- 사용자는 화면마다 현재 내용, 실행할 동작, 필요한 상태와 오류 해결에 필요한 문구만 확인할 수 있다.

## 반드시 지킬 조건

- 위 과업은 LLM을 사용하지 않고 완료할 수 있어야 한다.
- 로컬 모드는 별도의 애플리케이션 백엔드나 데이터베이스 서버에 연결하지 않아도 현재 범위의 모든 과업을 완료할 수 있어야 한다. 로컬 모드라는 이름은 정적 HTML 산출물이나 특정 Next.js 배포 형식을 뜻하지 않는다. 현재 과업에 필요하다면 같은 애플리케이션의 Next.js Server Actions와 Route Handlers를 사용할 수 있다.
- 독립 실행 애플리케이션은 정적 사이트 산출물을 만들 필요가 없다. 현재 배포 형식은 `next build`로 만든 운영용 빌드를 `next start`로 실행하는 Next.js Node.js 서버이며, 이 실행 과정에 별도의 애플리케이션 백엔드나 데이터베이스 서버를 추가하지 않아야 한다.
- 로컬 모드는 HTTPS URL 또는 호스트 이름이 `localhost`인 HTTP URL로 접속해야 한다. `file://` URL과 `localhost`가 아닌 호스트의 HTTP URL은 지원하지 않아야 한다.
- 지원하는 접속 주소에서 애플리케이션이 만든 서버 HTML과 첫 브라우저 렌더링이 달라지는 hydration 오류 없이 메모 화면이 열려야 한다.
- 개인 메모 애플리케이션 프로젝트는 `apps/notes/`에 두고, 애플리케이션의 패키지 매니페스트는 `apps/notes/package.json`이어야 한다.
- 프론트엔드 소스 구조는 FSD의 계층별 의존 방향과 slice public API 규칙을 따라야 한다. Next.js 라우트 디렉터리와 FSD의 App 및 Pages 계층은 서로 구분할 수 있어야 한다. 정적 import와 동적 import에서 판정할 수 있는 FSD 위반은 ESLint가 실패로 알려야 한다.
- 검사 도구를 한 번 확인하려는 fixture나 별도 실행 스크립트를 저장소에 남기지 않아야 한다. fixture와 스크립트는 반복해서 실행할 사용자 동작, 자료 규칙 또는 기존 도구만으로 표현할 수 없는 검사를 직접 검증할 때만 추가해야 한다.
- 애플리케이션은 Tailwind CSS를 스타일 작성 기반으로 사용하고, 의미가 정해진 디자인 토큰과 자체 `shared/ui` 구성요소로 고유 디자인 시스템을 만들어야 한다. 외부 UI 라이브러리의 기본 테마를 애플리케이션의 시각 기준으로 사용하지 않아야 한다.
- 제목, 본문, 버튼, 입력과 상태 문구를 포함한 인터페이스 텍스트는 Pretendard를 기본 글꼴로 사용하고 serif display 글꼴을 사용하지 않아야 한다. 정보 위계는 다른 글꼴 계열이 아니라 크기와 굵기로 구분해야 한다.
- 화면의 탐색, 도구와 패널은 Figma Design 편집기처럼 중립적인 애플리케이션 프레임, 얇은 구획선, 조밀한 도구 배치와 선택한 객체를 중심으로 바뀌는 제어를 사용해야 한다. Figma의 로고, 아이콘이나 고유 브랜드 색을 복제하는 것은 요구하지 않는다.
- 메모 작업 캔버스는 낮은 대비의 색과 결로 밝은 목재 인상을 줘야 한다. 메모와 제어 요소는 캔버스와 구분되는 중립적인 표면을 사용하고, 종이 카드나 짙은 목재 표현을 애플리케이션 전체의 시각 주제로 사용하지 않아야 한다.
- 일반 폼 제어는 네이티브 `button`, `input`, `textarea`와 `select`를 바탕으로 만들고, checkbox는 `input[type="checkbox"]`로 렌더링해야 한다. 모든 폼 제어에는 고유 디자인 시스템의 스타일을 적용하며 브라우저 기본 외형을 그대로 보여주지 않아야 한다.
- 폼 값을 다루지 않는 복합 상호작용에만 외부 접근성 부품을 선택적으로 사용할 수 있다. 실제 요구와 네이티브 Web API의 한계를 먼저 확인하고, 선택한 부품은 자체 `shared/ui` 구성요소 안에서만 사용해야 한다.
- 화면에 보이는 문구는 현재 내용이나 이동할 곳을 식별하거나, 입력과 동작의 이름을 붙이거나, 바로 알아차리기 어려운 상태 및 결과를 알리거나, 작업 완료에 필요한 조건과 다음 행동을 설명해야 한다. 제목, 컨트롤 이름과 배치가 이미 전달하는 역할을 되풀이하는 안내, 구현 기술 및 빌드 상태 설명과 현재 과업에 필요하지 않은 소개 문구는 표시하지 않아야 한다. 접근 가능한 이름, 입력 조건과 오류 해결처럼 사용자가 과업을 완료하는 데 필요한 문구는 유지해야 한다.
- 사용자에게 보이는 기능 이름, 화면 제목, 버튼, 설정과 상태 문구에는 `일괄 복사`를 사용하고 `누적 복사`를 사용하지 않아야 한다. 선택한 텍스트가 조작 순서대로 자료에 추가되는 규칙을 설명할 때에는 “누적”을 일반 동사로 사용할 수 있다.
- 로컬 모드의 줄 단위 텍스트 분석은 사용자가 분석 실행을 명시적으로 요청했을 때만 시작해야 한다.
- 현재 로컬 애플리케이션에는 계정 생성, 로그인, 동기화, 별도 백엔드 또는 데이터베이스 연결을 위한 UI와 기능을 포함하지 않아야 한다.
- 메모 화면에는 왼쪽 사이드바를 표시하지 않아야 한다. 다른 화면으로 이동하는 탐색은 메모 작업 영역의 가로 폭을 계속 차지하지 않는 위치에서 제공해야 한다.
- 메모 화면은 전역 상단 탐색 바로 아래에서 캔버스를 시작해야 한다. `메모` 페이지 제목, 메모 개수 또는 별도 상단 작업 행이 캔버스의 높이를 차지하지 않아야 하며, 캔버스는 상단 탐색을 제외한 보이는 화면의 너비와 높이를 채워야 한다.
- 메모 작업 영역의 inline size가 `48rem` 이상이면 `일괄 복사` 제어를 화면 우측 상단의 같은 위치에 유지하고 캔버스 배치에 별도 행을 만들지 않아야 한다. 캔버스를 이동하거나 스크롤해도 제어에 접근할 수 있어야 한다.
- 사용자가 첫 텍스트를 일괄 복사 목록에 추가하면 메모와 패널을 함께 표시할 공간이 충분한 화면에서는 선택 결과가 우측 패널에 바로 나타나야 한다. 패널이 열린 뒤 캔버스는 남은 영역을 채워야 하며 항목 추가로 현재 메모의 포커스를 옮기지 않아야 한다.
- 메모 작업 영역의 inline size가 `48rem` 미만이면 공간형 메모 화면 대신 생성 순서의 목록 형태를 제공하고, 메모 상세 이동, 길게 누르기 개별 복사와 일괄 복사 상태를 사용할 수 있어야 한다.
- 목록 전환 여부를 기기 종류나 User-Agent로 판단하지 않아야 하며, 목록으로 전환하거나 공간형 화면으로 돌아와도 저장된 메모 위치와 크기를 바꾸지 않아야 한다.
- 이 문서의 모바일 목록 표현은 기기 종류가 아니라 메모 작업 영역의 inline size가 `48rem` 미만인 표현을 뜻해야 한다.
- 모바일 목록 표현의 각 메모는 동일한 의미 기반 최대 세로 길이를 적용해야 한다. 원문이 제한을 넘으면 목록 카드 안에 별도 스크롤 영역을 만들지 않고 일부만 보여주되, 메모 상세 화면에서는 전체 원문을 확인할 수 있어야 한다.
- 모바일 목록 표현에서 메모를 일반적으로 짧게 누르면 해당 메모의 상세 화면으로 이동해야 한다. 이 동작은 메모 전체 복사나 일괄 복사 항목 추가를 함께 실행하지 않아야 한다.
- 모바일 목록 표현에서 메모를 길게 누른 뒤 같은 메모 안에서 손을 떼면 해당 메모의 원문 전체를 클립보드에 써야 한다. 길게 누르기로 복사가 완료되면 뒤이어 발생하는 짧은 누르기 또는 `click`이 상세 화면 이동을 실행하지 않아야 한다.
- 모바일 목록 표현의 메모 상세 화면에서는 전체 원문을 편집하고 명시적으로 저장할 수 있어야 한다. 목록 카드 자체는 편집 입력을 제공하지 않아야 한다.
- 모바일 목록 표현의 평상시 화면 헤더 오른쪽에는 일괄 복사 상태를 시작하는 아이콘 버튼을 제공해야 한다. 버튼은 화면에 텍스트를 표시하지 않더라도 기능을 설명하는 접근 가능한 이름을 제공해야 한다.
- 일괄 복사 상태에서는 평상시 헤더 아이콘 대신 헤더 왼쪽에 뒤로가기 아이콘 버튼을 제공해야 한다. 이 버튼은 화면 아래의 취소 버튼을 대신하며, 실행하면 클립보드를 바꾸거나 다음 단계로 이동하지 않고 일괄 복사 상태를 끝내야 한다.
- 일괄 복사 상태에서는 메모를 짧게 누를 때 상세 화면으로 이동하지 않고 누른 순서대로 현재 작업 목록에 추가해야 한다. 같은 메모를 반복해서 누르면 누를 때마다 별도 항목으로 추가되고 현재 클릭 횟수도 한 번씩 증가해야 한다.
- 일괄 복사 상태의 화면 아래에는 `초기화`와 `다음` 버튼을 고정해야 한다. `초기화`는 상태를 끝내지 않은 채 이번 작업 목록과 클릭 횟수를 0으로 만들고, `다음` 텍스트 옆에는 현재 클릭 횟수를 표시해야 한다. 이 횟수는 보조 기술에도 전달해야 한다.
- 일괄 복사 상태와 현재 작업에 추가된 메모는 색에만 의존하지 않는 방식으로 구분하고 보조 기술에도 전달해야 한다.
- 모바일 목록 표현에서 저장된 일괄 복사 항목이 하나 이상이면 화면 우측 하단에 현재 항목 수를 표시하는 고정 제어가 나타나야 한다. 항목 수는 보조 기술에도 전달해야 하며, 이 제어를 실행하면 일괄 복사 관리 페이지로 이동해야 한다.
- 메모 작업 영역의 inline size가 `48rem` 이상이면 메모 본문은 항상 편집 가능한 여러 줄 일반 텍스트 입력이어야 한다. 별도 읽기 상태, 편집 시작 버튼, 완료 버튼과 저장 버튼을 표시하지 않아야 하며 입력한 내용은 자동으로 저장해야 한다. `48rem` 미만의 목록 표현에서는 상세 화면에서만 편집과 명시적 저장을 제공해야 한다.
- `48rem` 이상의 공간형 화면에서 각 메모에는 본문과 구분되는 짧은 세로 높이의 헤더를 표시해야 한다. 헤더 안에는 설명 문구나 `이동`, `복사`, `누적`, `편집`, `크기` 텍스트 버튼을 표시하지 않아야 한다.
- 공간형 화면의 메모 헤더에서 아이콘 버튼이 아닌 부분을 드래그하면 메모 위치가 바뀌어야 한다. 헤더를 클릭하면 해당 메모를 선택하되 메모 원문 개별 복사나 일괄 복사 목록 추가를 실행하지 않아야 한다.
- 공간형 화면의 메모 헤더 오른쪽에는 메모를 맨 앞으로 보내기, 맨 뒤로 보내기와 삭제하기를 실행하는 아이콘 전용 버튼을 이 순서로 배치해야 한다. 각 버튼은 화면에 텍스트를 표시하지 않더라도 고유한 접근 가능한 이름을 제공하고 `Enter`와 `Space`로 실행할 수 있어야 한다.
- 공간형 화면에서 메모를 맨 앞으로 보내면 다른 모든 메모보다 위에 표시되고, 맨 뒤로 보내면 다른 모든 메모보다 아래에 표시되어야 한다. 바뀐 겹침 순서는 새로고침 뒤에도 유지되어야 하며 메모 원문 전용 revision은 바뀌지 않아야 한다.
- 공간형 화면의 메모에서 직접 크기 조절을 시작할 수 있는 곳은 메모의 위, 아래, 왼쪽 또는 오른쪽 가장자리와 네 꼭짓점이어야 한다. 크기 조절 위치에는 동작을 설명하는 텍스트 버튼을 표시하지 않아야 한다. 선택한 메모의 오른쪽 속성 패널에서는 같은 너비와 높이를 수치로 바꿀 수 있어야 한다.
- 공간형 화면의 메모는 키보드의 `Tab`으로 도달해 선택할 수 있어야 한다. 선택한 메모에는 얇은 붉은 테두리를 표시하고, 현재 키보드 포커스는 선택 테두리와 별도로 구분할 수 있어야 한다.
- 선택한 메모의 오른쪽 속성 패널에서는 작업 공간 좌표 X와 Y, 너비와 높이를 바꿀 수 있어야 한다. 메모 표면에는 이 수치 입력이나 별도 설정 버튼을 표시하지 않아야 한다.
- 오른쪽 속성 패널에서 바꾼 값은 해당 입력의 `blur`, 패널 밖 클릭 또는 `Enter`가 발생할 때 적용해야 한다. 잘못된 입력은 저장된 메모 좌표와 크기를 바꾸지 않고 사용자가 같은 입력에서 수정할 수 있게 알려야 한다.
- 넓은 메모 화면의 본문을 일반 클릭하거나 drag하면 커서 이동과 텍스트 선택만 수행해야 한다. 일반 클릭으로 메모 전체 복사, 일괄 복사 목록 추가 또는 별도 편집 상태 전환을 실행하지 않아야 한다.
- 사용자가 정한 메모 크기보다 내용이 길면 메모 안에서 내용을 스크롤할 수 있어야 한다.
- 메모 본문의 URL이나 링크처럼 보이는 문자열은 일반 텍스트로 취급해야 하며, 초기 범위에서는 자동 링크 변환, 클릭 이동, 미리보기 또는 별도 링크 UI를 제공하지 않아야 한다.
- 클립보드 권한 거부나 쓰기 실패로 복사를 완료하지 못하면 사용자가 실패 원인과 다음 행동을 알 수 있는 알림 UI를 제공해야 한다.
- 메모 본문을 `Command+클릭`하면 클릭한 메모의 원문 전체를 클립보드에 복사해야 한다. 복사가 성공했을 때만 개별 복사 횟수를 기록해야 한다.
- 메모 본문을 `Command+Option+클릭`하면 개별 복사를 함께 실행하지 않고 클릭한 메모의 원문을 일괄 복사 목록에 추가해야 한다. 두 modifier가 모두 눌린 조합은 `Command+클릭`보다 먼저 판정해야 한다.
- `Command+클릭` 개별 복사와 `Command+Option+클릭` 일괄 복사 추가는 넓은 화면의 메모 본문에만 적용해야 한다. 헤더, 헤더 아이콘 버튼, 탐색 항목과 다른 조작 요소의 클릭 결과를 바꾸지 않아야 한다.
- 별도 설정 화면에서 `Command+Option+클릭` 일괄 복사 추가를 켜거나 끌 수 있어야 하며, `Ctrl`, `Shift` 또는 `Option+클릭`만을 자동 대체 동작으로 지정하지 않아야 한다.
- 서로 다른 여러 메모에서 선택한 텍스트를 하나의 일괄 복사 목록에 추가할 수 있어야 한다.
- 일괄 복사 목록에 추가할 때의 메모 원문을 스냅샷으로 보관하고, 같은 텍스트를 여러 번 추가할 수 있어야 한다.
- 일괄 복사 항목 사이의 기본 구분자는 줄바꿈이어야 한다.
- 일괄 복사 목록의 최초 순서는 사용자가 항목을 추가한 순서와 같아야 한다.
- 사용자가 일괄 복사 항목을 드래그 앤 드롭하면 놓은 위치에 따라 순서가 바뀌어야 한다.
- 사용자가 특정 항목을 일괄 복사 목록 밖에 놓으면 드래그한 항목이 목록에서 제거되어야 한다.
- 일괄 복사 항목이 목록 밖으로 이동하면 제거 예정 상태를 보여줘야 하며, `Escape` 또는 pointer 취소가 발생하면 제거하지 않아야 한다.
- 넓은 메모 화면의 일괄 복사 패널에서는 항목 순서를 드래그 앤 드롭으로만 바꿔야 한다. 각 항목에 순서 번호, 위로 이동 또는 아래로 이동 버튼을 표시하지 않아야 한다.
- 넓은 메모 화면의 일괄 복사 패널에서는 항목에 pointer를 올리면 해당 항목의 우측 상단 꼭짓점에 제거 아이콘 버튼이 나타나야 한다. 항목에서 pointer가 벗어나면 제거 아이콘 버튼을 계속 표시하지 않아야 한다.
- 사용자가 일괄 복사 항목 제거를 실행 취소하면 제거된 텍스트가 이전 위치에 돌아와야 한다.
- 사용자가 실행 취소한 제거를 다시 실행하면 복원됐던 텍스트가 다시 제거되어야 한다.
- 제거 실행 취소와 다시 실행 이력은 애플리케이션 안에서 페이지를 이동해도 유지하되, 새로고침하면 지워져야 한다. 실행 취소 뒤 일괄 복사 목록을 다시 바꾸면 다시 실행 이력을 지워야 한다.
- 메모 작업 영역의 inline size가 `48rem` 이상이면 일괄 복사 목록을 메모 화면에서 열고 닫을 수 있는 보조 패널로 확인해야 한다. 메모와 일괄 복사 패널을 함께 표시하기에 충분하면 두 영역을 나란히 사용하고, 공간이 부족하면 같은 메모 화면 위의 모달 패널로 전환해야 한다.
- 넓은 메모 화면의 일괄 복사 패널에는 실행 취소와 다시 실행 버튼을 표시하지 않아야 한다. 제거 이력과 키보드 단축키 동작은 유지해야 한다.
- 넓은 메모 화면의 일괄 복사 패널에서 `일괄 복사` 제목과 닫기 동작이 있는 머리 영역은 변경 전 계산 높이인 `63 CSS px`의 60%인 약 `38 CSS px` 높이로 줄여야 한다.
- 넓은 메모 화면의 일괄 복사 패널에는 항목을 하나로 합친 별도 미리보기 영역을 표시하지 않아야 한다. 전체 복사 동작은 현재 항목 순서와 줄바꿈 구분자로 합친 문자열을 계속 복사해야 한다.
- 일괄 복사 항목 추가에 성공했을 때 `누적했습니다`와 같은 별도 성공 알림을 표시하거나 읽어주지 않아야 한다. 항목, 일괄 복사 상태와 항목 수가 바뀐 결과로 성공을 확인할 수 있어야 한다.
- 일괄 복사 패널에서 전체 복사를 실행한 뒤의 성공 또는 실패 알림은 전역 상단 탐색과 일괄 복사 패널을 제외한 메모 작업 영역의 우측 상단에 고정해야 한다. 이 알림은 이동 및 확대되는 캔버스의 하위 요소가 아니어야 하며, 패널을 열거나 캔버스를 이동해도 같은 화면 위치를 유지해야 한다.
- 메모가 아닌 빈 캔버스에서 시작한 주 pointer 드래그는 캔버스 시점을 이동해야 한다. 메모 본문, 메모 가장자리 및 꼭짓점, 일괄 복사 패널이나 다른 조작 요소에서 시작한 pointer 동작은 캔버스 이동으로 해석하지 않아야 한다.
- 헤더의 삭제 아이콘을 실행하면 확인 대화상자를 먼저 표시하지 않고 대상 메모만 활성 메모 목록에서 제거해야 한다. 다른 메모의 원문, 위치, 크기와 상대 겹침 순서는 바꾸지 않아야 한다.
- 메모를 제거한 직후 전역 상단 탐색, 오른쪽 패널과 변환되는 캔버스를 제외한 메모 작업 주 영역의 중앙 하단에 `메모를 제거했습니다.`를 표시해야 한다. 같은 알림의 오른쪽에는 `취소` 버튼을 제공해야 한다.
- 제거 알림은 나타날 때 현재 키보드 포커스를 강제로 옮기지 않아야 한다. `취소`를 실행하면 제거한 메모의 원문, 위치, 크기와 겹침 순서를 제거 전 상태로 복원해야 한다.
- 메모 작업 영역의 inline size가 `48rem` 미만이면 저장된 일괄 복사 목록을 별도 `/accumulator` 페이지에서 관리해야 한다. 이 페이지는 전역 탐색 항목으로 추가하지 않고 메모 화면 우측 하단의 항목 수 제어로 이동해야 한다.
- 모바일 일괄 복사 관리 페이지는 현재 선택 순서의 목록을 제공해야 한다. 각 항목의 왼쪽에는 재정렬을 시작하는 별도 버튼을 두고, 드래그 없이 실행하는 위로 이동, 아래로 이동과 제거 버튼도 유지해야 한다.
- 모바일 일괄 복사 관리 페이지의 하단에는 `취소`와 `일괄 복사하기` 버튼을 두어야 한다. `취소`는 텍스트를 복사하지 않고 메모 화면으로 돌아가며 현재 항목과 순서를 유지해야 한다. `일괄 복사하기`는 현재 순서의 텍스트를 줄바꿈으로 합쳐 클립보드에 쓴 뒤 항목, 순서와 사용 횟수를 바꾸지 않아야 한다.
- 일괄 복사 패널과 모바일 일괄 복사 관리 페이지의 표현은 기기 종류나 User-Agent가 아니라 실제 작업 영역의 inline size로 정해야 한다.
- 텍스트 사용 빈도와 줄 단위 텍스트 분석 결과는 메모 입력 화면과 별도의 화면에서 확인할 수 있어야 한다.
- 사용 빈도 화면은 메모와 조작 당시 원문 상태별로 개별 복사 횟수, 일괄 복사 항목 추가 횟수와 두 횟수에서 계산한 합계를 구분해 보여줘야 한다.
- 개별 복사는 `Command+클릭` 또는 모바일 목록의 길게 누르기로 클립보드 쓰기가 성공했을 때만 횟수에 포함하고, 일괄 복사는 항목 추가와 횟수 저장이 모두 성공했을 때만 포함해야 한다. 제거, 순서 변경, 실행 취소와 다시 실행은 횟수를 바꾸지 않아야 한다.
- 텍스트 분석의 입력 단위는 메모 전체가 아니라 메모의 원문을 줄바꿈으로 나눈 각 줄이어야 한다.
- 비교용 줄은 앞뒤 공백 제거, 내부 공백 정리, NFC 정규화와 대소문자 무시를 적용하되 원문을 바꾸지 않아야 한다. 정리한 결과가 빈 줄이면 분석에서 제외해야 한다.
- 생김새가 겹치는 텍스트는 정확한 반복, 포함 관계와 문자 3-gram Jaccard 점수를 사용하는 문자열 근접 후보로 구분할 수 있어야 한다.
- 문자열 근접 점수는 의미가 같다는 판정이나 고정된 합격 임계값으로 사용하지 않아야 한다. 0보다 큰 점수의 후보를 점수가 높은 순서로 보여주고, 3개 미만의 사용자 인식 문자로 된 줄은 정확한 반복과 포함 관계만 판정해야 한다.
- 텍스트가 바뀌지 않은 위치와 크기 변경은 사용 빈도를 새 항목으로 나누거나 기존 분석 결과를 오래된 것으로 만들지 않아야 한다.
- 템플릿 제안은 사용자가 선택한 두 원문 줄의 공통 구간을 일반 텍스트로, 다른 연속 구간을 플레이스홀더로 제시해야 한다. 제안은 사용자가 저장하기 전까지 저장된 템플릿으로 취급하지 않아야 한다.
- 공통 일반 텍스트가 없거나 공백과 문장부호만 공통인 두 줄, 또는 차이 구간이 없는 같은 두 줄에는 자동 템플릿을 만들지 않고 이유와 수동 템플릿 작성 동작을 보여줘야 한다.
- 사용자가 한 템플릿 안의 여러 부분을 플레이스홀더로 지정할 수 있어야 한다.
- 서비스는 흔한 AI SaaS UI처럼 보여서는 안된다. 현대적인 웹 애플리케이션의 디자인 트렌드를 반영해야 한다.

## 완료를 확인할 증거

- 사용자가 메모를 두 개 이상 만들고 각 메모에 서로 다른 텍스트를 입력할 수 있다.
- 사용자가 다른 애플리케이션에서 복사한 텍스트를 메모에 붙여넣고 붙여넣은 내용을 확인할 수 있다.
- `48rem` 이상의 메모 작업 영역에서 사용자가 메모 두 개의 위치와 크기를 각각 바꾼 뒤에도 두 메모의 내용과 배치가 유지된다.
- 메모 화면을 넓은 화면과 320 CSS px 너비에서 각각 열었을 때 왼쪽 사이드바가 없고, 메모 작업 영역을 가리지 않으면서 다른 화면으로 이동할 수 있다.
- 넓은 메모 화면을 열었을 때 전역 상단 탐색 바로 아래에서 밝은 목재 캔버스가 시작해 보이는 화면의 나머지 너비와 높이를 채우며, 별도의 `메모` 제목, 메모 개수 또는 상단 작업 행이 보이지 않는다.
- `48rem` 이상의 메모 캔버스를 스크롤하거나 이동해도 `일괄 복사` 제어가 화면 우측 상단에 남고 캔버스의 시작 위치나 높이를 바꾸지 않는다.
- `48rem` 미만의 메모 작업 영역에서 메모가 공간형 화면 대신 생성 순서의 제한된 높이 목록으로 표시된다. 일반 터치로 선택한 메모의 상세 화면에 이동해 전체 원문을 편집하고 저장할 수 있으며, 작업 영역을 다시 넓히면 기존 공간 배치가 복원된다.
- 넓은 화면에서 메모가 표시되는 동안 본문은 별도 전환 없이 계속 편집할 수 있고, 완료 또는 저장 버튼이 없어도 입력한 원문이 새로고침 뒤 유지된다.
- 넓은 화면에서 사용자가 메모 본문을 일반 클릭하거나 drag하면 커서 또는 선택 범위가 바뀌고, 메모 원문 전체가 클립보드에 복사되지 않는다.
- 넓은 화면의 각 메모에는 짧은 헤더와 본문만 보인다. 헤더에는 오른쪽의 맨 앞으로, 맨 뒤로 및 삭제 아이콘 버튼 외에 `이동`, `복사`, `누적`, `편집`, `크기`, 숫자 입력 또는 단계 조절 UI가 보이지 않는다. 좌표와 크기 입력은 선택한 메모의 오른쪽 속성 패널에만 보인다.
- 사용자가 헤더의 아이콘이 아닌 부분을 drag하면 메모 위치가 바뀐다. 헤더를 클릭하기만 하면 메모가 선택되고 얇은 붉은 테두리와 오른쪽 속성 패널이 나타나며 메모 원문은 복사되지 않는다.
- 사용자가 `Tab`으로 메모에 도달하면 그 메모가 선택된다. 선택 테두리와 현재 `focus-visible` 표시를 동시에 구분할 수 있고, 속성 패널로 포커스를 옮겨도 선택한 메모는 바뀌지 않는다.
- 사용자가 겹친 메모 가운데 하나의 맨 앞으로 아이콘을 실행하면 그 메모가 다른 모든 메모 위에 나타나고, 맨 뒤로 아이콘을 실행하면 다른 모든 메모 아래에 나타난다. 새로고침 뒤에도 같은 겹침 순서가 유지된다.
- 사용자가 삭제 아이콘을 실행하면 대상 메모만 화면과 저장 자료의 활성 메모 목록에서 사라지고 주 작업 영역 중앙 하단에 `메모를 제거했습니다.`와 `취소`가 나타난다. `취소`를 실행하면 같은 원문, 위치, 크기와 겹침 순서의 메모가 복원된다.
- 사용자가 메모의 네 가장자리와 네 꼭짓점을 각각 드래그했을 때 해당 방향으로만 크기가 바뀐다. 선택한 메모의 오른쪽 속성 패널에서는 X, Y, 너비와 높이를 입력할 수 있지만 메모 표면에는 숫자 입력이나 단계 조절 UI가 나타나지 않는다.
- 사용자가 오른쪽 속성 패널의 X, Y, 너비 또는 높이를 바꾸고 각 입력의 `blur`, 패널 밖 클릭과 `Enter`를 각각 실행하면 유효한 값은 같은 메모에 한 번 적용된다. 유효하지 않은 값은 저장 geometry를 바꾸지 않고 수정할 입력을 알려준다.
- 메모에 URL 형식 문자열을 입력해도 별도 링크 UI가 생기거나 클릭 이동이 실행되지 않고, `Command+클릭`하면 URL을 포함한 메모 원문 전체가 복사된다.
- 클립보드 쓰기가 거부되거나 실패했을 때 알림 UI가 실패 사실과 다시 시도하거나 설정을 확인할 방법을 보여준다.
- 사용자가 서로 다른 메모 본문을 `Command+클릭`하면 각 원문이 개별적으로 복사되고, `Command+Option+클릭`하면 다른 화면으로 이동하거나 개별 복사를 함께 실행하지 않은 채 일괄 복사 패널에 조작 순서대로 추가된다.
- 넓은 메모 화면에서 첫 텍스트를 일괄 복사 목록에 추가하면 우측 패널이 나타나고 방금 선택한 원문이 보이며, 메모의 현재 포커스는 유지되고 캔버스는 패널을 제외한 나머지 영역을 채운다.
- 텍스트를 일괄 복사 목록에 추가해도 `누적했습니다`와 같은 별도 성공 알림이 나타나거나 보조 기술에 중복해서 전달되지 않고, 항목, 일괄 복사 상태와 항목 수 변화로 결과를 확인할 수 있다.
- 320 CSS px 너비에서 메모를 짧게 누르면 해당 상세 화면으로 이동하고 원문 전체를 편집해 저장할 수 있다. 같은 메모를 길게 눌렀다가 카드 안에서 손을 떼면 원문 전체가 한 번 복사되며 상세 화면으로 이동하지 않는다.
- 320 CSS px 너비에서 메모 원문이 카드의 최대 세로 길이를 넘어도 목록 행의 높이는 정한 token을 넘지 않고, 상세 화면에서 잘린 원문 전체를 확인할 수 있다.
- 320 CSS px 너비에서 평상시 화면 헤더 오른쪽의 일괄 복사 아이콘을 실행하면 헤더 왼쪽에 뒤로가기 아이콘이 나타나고 일괄 복사 상태가 시작된다. 첫 메모를 두 번, 둘째 메모를 한 번 짧게 누르면 상세 화면으로 이동하지 않고 세 원문 스냅샷이 같은 순서와 중복을 유지해 현재 작업에 추가되며, 화면 아래에는 `초기화`와 클릭 횟수 3이 붙은 `다음`이 나타난다.
- 320 CSS px의 일괄 복사 상태에서 `초기화`를 실행하면 상태를 유지한 채 현재 작업과 클릭 횟수가 0이 된다. 헤더의 뒤로가기를 실행하면 클립보드를 바꾸거나 다음 단계로 이동하지 않고 평상시 메모 목록으로 돌아간다.
- 320 CSS px 너비에서 우측 하단의 항목 수 제어를 실행하면 `/accumulator` 페이지로 이동한다. 각 항목의 왼쪽 버튼으로 순서를 바꿀 수 있고, 하단 `취소`는 복사 없이 메모 화면으로 돌아가며 `일괄 복사하기`는 현재 목록 순서의 텍스트를 줄바꿈으로 합쳐 클립보드에 쓴다.
- 사용자가 일괄 복사 목록의 순서를 드래그 앤 드롭으로 바꾸면 전체 복사 결과에 바뀐 순서가 반영된다.
- 넓은 메모 화면의 일괄 복사 패널에는 실행 취소, 다시 실행, 위로 이동, 아래로 이동, 순서 번호와 합친 텍스트 미리보기가 보이지 않는다. 순서를 바꾸는 동작은 항목 드래그로만 실행된다.
- 넓은 메모 화면의 일괄 복사 항목에 pointer를 올리면 우측 상단 꼭짓점에 제거 아이콘 버튼이 나타나고, 그 버튼을 실행하면 해당 항목만 제거된다.
- 넓은 메모 화면의 일괄 복사 패널 머리 영역의 계산 높이가 변경 전 `63 CSS px`에서 약 `38 CSS px`로 줄어든다.
- 사용자가 특정 텍스트를 일괄 복사 목록 밖에 놓으면 드래그한 텍스트가 목록과 전체 복사 결과에서 제거된다.
- 사용자가 방금 제거한 일괄 복사 항목을 실행 취소하면 이전 위치에 복원되고, 다시 실행하면 같은 항목이 다시 제거된다.
- 사용자가 일괄 복사 패널을 닫았다가 다시 열거나 다른 화면을 다녀와도 제거 실행 취소를 사용할 수 있고, 새로고침한 뒤에는 이전 실행의 제거 이력을 사용할 수 없다.
- 일괄 복사 패널에서 전체 복사를 실행하면 성공 또는 원인별 실패 알림이 상단 탐색 아래, 패널 왼쪽의 메모 작업 영역 우측 상단에 나타난다. 캔버스를 이동하거나 확대해도 알림 위치는 바뀌지 않는다.
- 사용자가 메모와 다른 조작 요소가 없는 캔버스 지점에서 드래그를 시작하면 시작 지점이 캔버스 어느 부분이든 같은 거리와 방향으로 시점이 이동하고, 메모나 패널에서 시작한 드래그는 캔버스 시점을 움직이지 않는다.
- 같은 텍스트를 `Command+클릭` 또는 모바일 길게 누르기로 두 번 복사하고 일괄 복사 목록에 세 번 추가했을 때 사용 빈도 화면에 개별 복사 2회, 일괄 복사 3회와 합계 5회가 표시된다.
- 하나의 메모에 서로 다른 줄을 두 개 이상 입력하고 다른 메모에 그중 한 줄과 겹치는 줄을 입력했을 때, 분석 결과가 메모 전체가 아니라 줄별 관계를 보여준다.
- 정확한 반복, 포함 관계와 문자 조각이 일부 겹치는 두 줄을 각각 준비했을 때 사용자가 별도 화면에서 세 관계와 문자열 근접 점수를 구분해 확인할 수 있다.
- 생김새가 겹치는 텍스트가 있을 때 사용자가 서로 겹친 텍스트를 바탕으로 한 템플릿 제안을 확인할 수 있다.
- 사용자가 텍스트의 여러 부분을 플레이스홀더로 표시하고 플레이스홀더가 표시된 텍스트를 템플릿으로 만들 수 있다.
- 사용자가 각 플레이스홀더에 값을 입력했을 때 입력한 값이 반영된 일회성 텍스트를 확인할 수 있다.
- 로컬 모드에서 계정이나 동기화 UI가 보이지 않고, 애플리케이션 백엔드 연결 없이 사용자가 분석 실행을 누른 뒤 줄별 관계와 문자열 근접 점수를 확인할 수 있다.
- 독립 실행용 빌드 결과를 시작한 뒤 사용자가 넓은 화면의 메모 작성 및 상시 편집, `Command+클릭` 개별 복사와 `Command+Option+클릭` 일괄 복사 추가를 완료할 수 있다. 작은 화면에서는 목록 상세 이동, 상세 편집 및 저장, 길게 누르기 개별 복사와 일괄 복사 상태를 사용할 수 있다.
- 확장 기능이 HTML을 바꾸지 않는 지원 브라우저에서 독립 실행용 빌드의 메모 화면을 처음 열고 새로고침해도 hydration 경고와 오류 화면이 나타나지 않는다.
- 같은 revision의 독립 실행용 빌드를 호스트 이름이 `localhost`인 HTTP URL과 HTTPS URL에서 각각 열었을 때 넓은 화면의 메모 작성 및 상시 편집, `Command+클릭` 개별 복사와 `Command+Option+클릭` 일괄 복사 추가를 완료할 수 있다. localhost HTTP는 Next.js 운영 서버에서 확인하고, HTTPS는 해당 서버 앞에서 TLS를 종료하는 배포 환경에서 확인한다.
- `apps/notes/package.json`에서 독립 실행 애플리케이션의 의존성과 실행 명령을 확인할 수 있고, `apps/notes/`의 빌드 명령으로 실행 가능한 결과물을 만들 수 있다.
- ESLint 검사에서 FSD 계층의 역방향 import, 허용된 Entities `@x` public API 외의 같은 계층 import와 public API 우회가 발견되지 않고, Next.js 라우트 파일과 FSD의 App 및 Pages 계층을 파일 위치로 구분할 수 있다.
- 다섯 주요 route, 모바일 메모 상세 route, 모바일 일괄 복사 관리 route와 메모 화면의 일괄 복사 패널에서 같은 역할의 버튼, 입력과 알림이 같은 디자인 토큰 및 `shared/ui` 구성요소를 사용하고, 포커스 표현도 같은 규칙을 따른다. 실제 한국어 콘텐츠를 넣은 대표 구성요소에 적용 가능한 기본, hover, `focus-visible`, pressed, disabled, 오류와 진행 상태가 담당자의 시각 및 접근성 검토를 통과한다.
- 다섯 주요 route, 모바일 메모 상세 route와 모바일 일괄 복사 관리 route의 제목, 본문, 버튼, 입력과 상태 문구의 계산된 `font-family`에서 Pretendard가 기본 글꼴이며 serif display 글꼴이 사용되지 않는다. 글꼴을 불러오지 못한 경우에도 sans-serif 대체 글꼴을 사용한다.
- 넓은 메모 화면에서 중립적인 탐색 및 도구 영역, 밝은 목재 인상의 작업 캔버스와 중립색 메모가 서로 구분된다. 메모 화면에는 왼쪽 사이드바가 없고, Figma식 도구 배치를 적용하더라도 메모 작업 영역과 일괄 복사 패널의 가로 폭을 계속 우선한다.
- DOM 검사에서 일반 폼 제어가 네이티브 HTML 요소로 렌더링되고, 각 입력의 이름, 설명, 오류와 키보드 포커스를 보조 기술이 확인할 수 있다. 외부 접근성 부품을 추가했다면 폼 값을 다루지 않는 승인된 구성요소에서만 사용하며 외부 기본 테마가 화면에 보이지 않는다.
- 다섯 주요 route, 모바일 메모 상세 route, 모바일 일괄 복사 관리 route와 메모 화면의 일괄 복사 패널에서 사용자에게 보이는 문구를 역할별로 확인했을 때 제목이나 컨트롤을 되풀이하는 안내, 구현 기술 및 빌드 상태 설명과 동작에 필요하지 않은 소개 문구가 없다. 클립보드 쓰기처럼 결과가 화면에 직접 나타나지 않는 동작의 상태, 입력 조건, 접근 가능한 이름과 오류 뒤 다음 행동은 확인할 수 있다.

## 구현 전에 결정할 충돌

다음 항목은 새 요구의 결과 또는 자료 수명이 정해지지 않아 남은 사항이다. 조사에서 확인한 대안을 요구사항으로 승인한 것으로 간주하지 않으며, 이 조작의 구현을 다시 시작하기 전에 요구사항 책임자가 선택해야 한다.

- 모바일 일괄 복사 상태를 시작할 때 저장된 기존 목록을 이어 쓸지 빈 임시 목록으로 시작할지, `다음`이 기존 `/accumulator` 관리 페이지로 이동할지와 이번 작업을 저장 목록에 추가하거나 교체할지
- 모바일 일괄 복사 상태에서 헤더 뒤로가기를 실행한 뒤 이번 작업 목록을 다시 시작할 때 복원할지 지울지
- 모바일 일괄 복사 상태에서도 길게 누르기 개별 복사를 실행할지, 길게 누르기를 일괄 복사 상태의 다른 조작으로 사용할지
- 모바일 상세 화면에서 저장하지 않은 입력을 둔 채 뒤로 이동하거나 route를 벗어날 때 이동을 막고 확인할지, 입력을 임시 보관할지와 저장 실패 뒤 원문을 유지하는 방식
- 제거 알림을 언제 닫을지, 여러 메모를 연속으로 제거했을 때 취소 대상을 쌓을지 마지막 제거만 유지할지와 새로고침 뒤에도 취소할 수 있게 할지
- 자동 저장을 시작할 입력 시점과 대기 시간, 다른 메모로 포커스를 옮기거나 페이지를 나갈 때 대기 중인 내용을 저장하는 방식 및 저장 실패를 본문 조작을 가리지 않고 알리는 방식
- 넓은 일괄 복사 패널의 순서 변경을 드래그로만 제한하는 조건을 유지할지, WCAG 2.2의 드래그 없는 단일 pointer 대안과 키보드 동작을 보이지 않는 메뉴나 단축키로 함께 제공할지
- hover가 없는 환경과 키보드 탐색에서 넓은 일괄 복사 패널의 제거 아이콘 버튼을 나타내고 실행하는 방식
- 메모의 Tab 순서, 빈 캔버스 또는 `Escape`로 선택을 해제할지와 선택한 메모를 제거한 뒤 포커스 및 선택을 옮길 대상
- 일괄 복사 패널이 열린 상태에서 메모를 선택하면 두 오른쪽 패널을 함께 표시할지, 하나의 문맥 패널 영역에서 전환할지와 전환 우선순위
- 오른쪽 속성 패널의 좌표 및 크기 허용 범위, 잘못된 입력에서 포커스 이탈을 허용할지와 `Escape`로 입력 초안을 되돌릴지

## 초기 범위에서 제외한 백로그

### 최우선: 계정 및 동기화

계정 생성, 로그인, 동기화, 백엔드와 데이터베이스는 모든 백로그 가운데 우선순위가 가장 높다. 현재 로컬 애플리케이션을 완성한 뒤 다른 백로그보다 먼저 진행한다.

이 백로그에는 다음 결과가 포함된다.

- 사용자는 계정을 만들고 로그인할 수 있다.
- 사용자는 계정에 연결된 메모, 일괄 복사 항목과 템플릿을 여러 브라우저에서 동기화할 수 있다.
- 백엔드와 데이터베이스가 인증, 접근 권한, 동기화 상태와 데이터 보관을 처리한다.
- 로컬과 계정 실행 형태에서 공통으로 제공하는 과업은 같은 화면 구조와 조작 방식을 사용한다.
- 계정 동기화처럼 로컬에서 제공하지 않는 과업과 조작 UI는 로컬 애플리케이션에 표시하지 않는다.

백로그 작업을 시작하기 전에 다음 사항을 정해야 한다.

- 계정 생성과 로그인 방식
- 로컬과 계정 실행 형태를 빌드 시점 설정으로 나눌지, 배포 시점 설정으로 나눌지와 기능 가용성을 어떤 단위로 표현할지
- 동기화 충돌 해결 규칙, 삭제 전파와 계정 데이터 삭제 범위
- 서버 API, 데이터베이스 구조와 여러 기기 사이의 일관성 기준
- 계정 기능을 추가한 뒤 로컬과 계정 실행 형태의 공통 화면이 같은 정보 구조와 조작을 유지하는지 확인할 방법

### 그 밖의 후속 항목

- LLM을 사용하지 않고 의미가 비슷한 텍스트를 자동으로 판별하는 동작은 초기 개발 범위에 포함하지 않고 후속 개발 항목으로 남긴다.
- 의미가 비슷하다고 판별한 텍스트를 바탕으로 한 템플릿 제안도 후속 개발 항목으로 남긴다.
- 메모 안 URL의 자동 링크 변환, 클릭 이동, 미리보기와 링크 정보 불러오기는 초기 개발 범위에 포함하지 않는다.
- 로컬 자료의 내보내기와 가져오기, 브라우저가 지운 자료의 복구는 초기 개발 범위에 포함하지 않는다.

### 라이브러리 및 포트폴리오 연결

개인 메모 애플리케이션을 라이브러리 패키지로 빌드하고 포트폴리오 웹 애플리케이션이 선택한 라우트에서 불러오는 작업은 백로그로 남긴다. 현재 구현 범위와 완료 증거에는 라이브러리 빌드 및 포트폴리오 라우트 연결을 포함하지 않는다.

백로그 작업을 시작하기 전에 다음 사항을 정해야 한다.

- 라이브러리 패키지를 같은 저장소의 workspace에서만 사용할지, 별도로 배포해 다른 저장소에서도 설치할 수 있게 할지
- 라이브러리가 지원할 호스트를 Next.js App Router로 제한할지, 다른 React 기반 호스트에서도 사용할 수 있게 할지
- 포트폴리오 웹 애플리케이션이 개인 메모의 단일 라우트만 연결할지, 일괄 복사, 사용 빈도, 텍스트 분석, 템플릿과 설정의 하위 라우트까지 연결할지
- 독립 실행 형태와 포트폴리오 라우트 연결 형태에서 바깥쪽 탐색, 페이지 제목, 테마와 알림 영역 가운데 무엇을 개인 메모 모듈이 그리고 무엇을 호스트가 그릴지
- 포트폴리오 라우트 연결 형태에서 로컬 모드만 제공할지, 계정 모드와 서버 쪽 연결도 함께 제공할지
- 라이브러리 빌드의 공개 진입점, 스타일과 Web Worker 파일 제공 방식 및 호스트 버전 호환 범위
