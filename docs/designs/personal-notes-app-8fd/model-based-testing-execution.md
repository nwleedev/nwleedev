# 메모 모델 기반 탐색 실행 결과

새 완료 기준에서 생성, 본문 편집과 모바일 명시적 저장의 세 기능은 로컬 다섯 절차의 증거를 확보했다. 나머지 9개 기능은 `needs revision`이다. 데스크톱 자동 저장은 실제 화면의 타이머와 이탈 입력을 다시 확인했으나 늦은 저장 완료의 실제 화면 증거가 남아 있다. 아래의 이전 실행 결과는 당시 구현의 기록이며 다른 기능의 현재 완료 판정을 대신하지 않는다. 앞선 기능의 완료를 다른 기능의 시작 조건으로 삼지 않는다. CI 재현은 이번 로컬 완료 판정에 포함하지 않는다.

## 생성 기능 실행 결과

2026년 9월 24일 실행에서 생성 기능의 로컬 판정은 `pass`다. `notes.spec.ts`는 실제 새 메모 버튼, 본문 입력 및 새로고침을 생성 명령으로 실행한다. 후보마다 독립 BrowserContext를 열고 화면 편집기와 새 IndexedDB 연결의 원문 목록 및 서로 다른 ID 수를 기대 입력에서 계산한 값과 비교한다. 첫 쓰기 중단 뒤 재시도는 별도 사용자 과업으로 유지한다. 두 번째 생성 쓰기만 성공처럼 누락하는 격리 조건에서는 같은 생성 명령과 관찰식이 화면과 저장 자료의 불일치를 찾는다. 운영 코드와 저장 자료 형식은 변경하지 않았다.

Chromium, Firefox 및 WebKit에서 정상 생성, 실패 뒤 재시도, 통제된 저장 누락의 아홉 E2E 검사가 통과했다. 각 실행은 seed, path, replayPath, 실제로 실행된 최초 및 축소 행동, 예상 원문과 조회된 원문을 Playwright 첨부 자료 및 출력에 남긴다. 대표적인 WebKit 실행에서는 seed `-995736752`, path `0:2:1:1:1:1`, replayPath `DAJ:F`에서 생성, 다시 열기, 생성, 생성의 네 행동이 생성 두 행동으로 축소됐다. 처음에는 예상 원문 세 개에 대해 저장 원문 두 개, 축소 뒤에는 예상 원문 두 개에 대해 저장 원문 하나를 읽었다. 축소된 두 행동을 같은 seed 및 path로 다시 생성해 위반을 재현했고, 난수 없이 실행해도 쓰기 누락 조건에서만 실패했다. 마지막 생성 행동을 빼면 누락이 발생하지 않았다. 이는 전역 최소 순서의 증명이나 실제 IndexedDB 결함 발생 기록이 아니다.

이 실행 전에 전체 Playwright E2E 175개가 통과했다. 생성 검사 변경 후에는 대상 아홉 검사를 세 브라우저에서 다시 실행했고 타입 검사와 lint도 통과했다. 전체 E2E는 이 변경 후 다시 실행하지 않았으므로 이전 전체 실행과 대상 재실행을 구분한다. 실제 기기 입력기 및 CI 재현은 이 결과에 포함하지 않는다.

### 이전 실행 기록

당시 생성 기능의 판정은 `needs revision`이며 행동 생성과 오류 탐지만 `증거 확보`였다. 사용자가 실제 서비스에서 실행하는 생성, 원문 입력, 연속 생성과 새로고침을 기존 `notes.spec.ts`에 연결했다. 후보마다 독립 BrowserContext를 만들고 생성 두 번과 최대 네 개의 생성 또는 새로고침 명령을 조합한다. 입력값은 fast-check가 생성하며 화면의 편집기 개수와 입력값을 같은 후보의 예상 원문 목록과 비교한다.

연속 생성 및 첫 쓰기 실패 뒤 재시도에서 새 IndexedDB 연결이 읽은 원문 목록과 서로 다른 ID 수를 입력 목록에 대조하고, 새로고침 뒤에도 같은 결과를 확인했다. 두 번째 메모의 쓰기만 성공처럼 누락시키는 격리 조건에서는 화면에 두 메모가 남았지만 새 저장소 연결에는 첫 메모만 남아 같은 관찰식이 실패했다. 두 검사의 Chromium, Firefox와 WebKit 실행 9개가 통과했다. 통제 결함은 운영 코드나 저장 자료 형식에 남지 않는다. 이 결과는 실패 행동의 축소, seed와 path 기록 또는 난수 없는 재현을 입증하지 않는다.

실패 뒤 재시도는 첫 IndexedDB 메모 쓰기만 중단하는 브라우저 문맥에서 실행했다. 사용자가 `새 메모` 버튼을 누른 뒤 작업 영역의 오류 알림과 메모가 늘지 않은 결과를 확인하고, 같은 버튼으로 다시 생성해 원문을 입력한 뒤 새로고침했다. 이 격리 조건은 실패 및 재시도 행동을 실제 UI에 연결하기 위한 것이며 저장 누락 탐지나 최소 실패 순서, 로그와 재현을 입증하지 않는다.

이전 생성 검사의 가짜 repository, 테스트 전용 화면 명령, `failed` 값과 고정된 최소 행동 검사를 삭제했다. `notes-model.spec.ts`에 있던 고정 원문 생성 검사와 두 문자열만 바꾸던 속성 검사를 제거하고 새 실제 화면 검사로 교체했다. 새 fixture, 별도 스크립트와 운영 상태는 추가하지 않았다.

수정 전 Playwright E2E 126개가 통과했다. 새 생성 검사 첫 실행에서는 작업 영역의 오류 알림과 Next.js route announcer를 함께 찾은 선택자 때문에 브라우저 세 개에서 실패했다. 작업 영역 안의 알림으로 관찰 대상을 한정한 뒤 두 생성 검사의 브라우저별 실행 6개가 Chromium, Firefox와 WebKit에서 모두 통과했다. 마지막 전체 실행은 126개 중 이번 변경과 무관한 WebKit 선택 및 포커스 검사 한 개가 실패하고 125개가 통과했으며, 실패한 검사만 다시 실행했을 때 통과했다. 이후 실제 저장 결과와 두 번째 쓰기 누락 탐지를 추가했다. 실패 순서 축소, 로그 기록과 재현 절차는 진행하지 않았다.

## 본문 편집과 원문 revision 실행 결과

2026년 9월 24일 새 절차의 로컬 판정은 `pass`다. `note.test.ts`에서 원문 교체, 공백을 포함한 입력, 줄바꿈, URL 모양 문자열, 원래 값 복귀, 같은 값 재적용과 X 위치 변경을 생성했다. 기대 모델은 앞선 기대 원문 및 revision과 이번 입력만 보관하며, 실제 결과는 `reviseNote`에서 읽었다. 정상 후보 100개가 통과했다. 공백을 제거하는 통제 결함과 위치만 바꿀 때 원문 revision을 증가시키는 통제 결함은 같은 관찰식에서 실패했다.

전체 단위 검사 실행에서 공백 손실은 seed `-934025517`, path `3:1:0`, replayPath `BEB:F`로 재실행했다. 처음 실패한 여러 행동은 공백만 있는 원문 교체 하나로 줄었고, 예상 원문은 공백 두 개였지만 통제 결함에서는 빈 문자열이었다. 위치 결함은 seed `-1589639760`, path `0:1:0`, replayPath `BFB:F`로 재실행했다. 축소된 행동은 X 변경 하나이며, 예상 원문 revision `2` 대신 통제 결함은 `3`을 관찰했다. 두 축소 행동을 난수 없이 다시 실행하면 각 결함에서만 실패하고 정상 함수에서는 통과한다. 숫자 revision은 해당 실행의 초기 자료에서 나온 값이며 일반 상수로 쓰지 않았다.

동일 책임의 이전 본문 탐색은 `note-lifecycle.model.test.ts`에서 제거했다. 배치, 삭제 및 복원 검사는 남겼다. 전체 단위 검사 36개 파일의 199개 검사, 타입 검사와 lint가 통과했다. 기존 실제 편집기 과업에는 축소에서 드러난 공백만 있는 원문을 추가해 저장소 재조회, 원문 revision과 새로고침 뒤의 값을 Chromium, Firefox 및 WebKit에서 확인했고 세 검사 모두 통과했다. 실제 IME 조합과 외부 앱 붙여넣기는 이 결과에 포함하지 않는다.

### 이전 실행 기록

당시 본문 편집과 원문 revision은 `pass`였다. `reviseNote`를 실행하는 독립 모델은 빈 원문, 공백, 여러 줄, Unicode, URL 모양 원문, 원래 값 복귀, 같은 값 반복과 위치 변경을 조합한다. 로컬 빠른 프로필의 정상 후보 100개에서 행동 471개를 실행했고 거절되거나 시간 상한으로 중단된 행동은 없다.

- 행동 생성: `edit-blank`, `edit-lines`, `edit-unicode`, `paste-url`, `restore-original`, `move-x`와 `inspect`를 `fc.commands`로 생성했다. 본문 행동은 적용할 전체 원문을 모델에 보관하고 위치 행동은 X 값만 바꿔 두 revision의 예상값을 독립적으로 계산한다.
- 오류 탐지: 본문 앞뒤 공백을 제거하는 결함은 예상 원문 `" "`과 관찰 원문 `""`의 차이를 탐지했다. 위치만 바꿀 때 content revision도 높이는 결함은 예상 revision 2와 관찰 revision 3의 차이를 같은 불변 조건 `content-revision-changes-only-for-content`에서 탐지했다.
- 실패 순서 축소: 공백 결함의 첫 실패 `move-x(30), paste-url, paste-url, edit-blank`는 `edit-blank` 하나로 줄었다. 위치 결함의 `inspect, move-x(30)`은 `move-x(30)` 하나로 줄었다. 두 실행 모두 한 번 축소됐고 문자열 축소와 행동 순서 축소를 구분했다.
- 로그 기록: 공백 결함은 seed `2`, path `2:1`, replayPath `E:B`, 위치 결함은 seed `2`, path `0:1`, replayPath `BEA:F`를 기록했다. 초기 및 예상 및 관찰 원문, content revision, 전체 revision, X 값, 앱과 모델 revision, 도구 버전, 프로필, 명령 집계, 소요 시간과 종료 이유도 JSON에 포함했다.
- 재현: 두 결함 모두 seed, path와 replayPath로 다시 실패했고 축소된 행동을 난수 없이 실행해 같은 불변 조건을 확인했다. 같은 두 행동은 정상 `reviseNote`에서 통과했다.

실제 애플리케이션에서는 textarea 전체를 선택해 앞뒤 공백, 줄바꿈과 URL 모양 문자열을 키보드로 입력했다. 저장된 IndexedDB 원문과 content revision을 읽어 본문 편집에서 두 revision이 한 번씩 증가하고, 이어 X만 바꾸면 전체 revision만 증가하는 것을 확인했다. URL은 링크가 되지 않았다. 이 검사는 Chromium, Firefox와 WebKit에서 통과했다. 외부 애플리케이션에서 가져온 Clipboard 붙여넣기는 이번 자동 검사로 입증했다고 기록하지 않는다.

공통 명령 계측은 생성과 편집 검사에서 실제 반복 책임이 확인된 뒤 재사용 함수로 옮겼다. Git revision 읽기도 Node와 브라우저 Vitest 설정이 같은 함수를 사용한다. 운영 화면, 메모 자료 형식과 오류 처리는 바꾸지 않았다. CI 재현은 원격 저장소와 CI 서비스가 정해진 뒤 별도로 확인한다.

## 데스크톱 자동 저장 실행 결과

2026년 9월 24일 새 절차의 현재 판정은 `needs revision`이다. 실제 메모 화면에서 원문 입력, 799ms, 1ms 및 800ms 경과, 재입력, blur와 저장 조회를 생성했다. 정상과 편집 후 IndexedDB 쓰기 누락 조건을 Chromium, Firefox 및 WebKit에서 실행한 여섯 검사가 통과했다. 누락 조건의 실패 행동은 입력과 저장 기한 도달로 축소됐고 seed, path, replayPath 재생 및 난수 없는 실행에서 같은 예상 원문과 저장 원문의 차이를 보였다. 정상 저장에서는 축소된 행동이 통과했다. 대표적인 축소는 WebKit의 `enter(" "),advance(799),advance(799)`이며, 특정 입력값이나 행동 수를 일반 규칙으로 가정하지 않는다.

실제 설정 링크를 누른 내부 이동은 저장 기한이 끝나기 전의 최신 원문을 저장했다. 숨김 및 pagehide 명령은 실제 메모 화면에 합성된 문서 이벤트를 전달해 처리 연결만 확인했다. 이 검사의 세 브라우저 실행 여섯 건이 통과했다. 브라우저 탭 전환 또는 OS 종료 자체를 확인한 결과는 아니다.

이전 Browser Mode의 테스트용 편집기 기반 타이머 명령과 중복 보고 코드는 제거했고 지연된 첫 저장 뒤 최신 입력을 남기는 예제는 유지했다. 지연 완료를 실제 메모 화면과 저장 자료에서 관찰하는 검사는 아직 없다. 따라서 아래 이전 탐색의 `pass`와 합쳐 현재 자동 저장을 완료로 계산하지 않는다. 남은 Browser Mode 아홉 검사, 타입 검사와 lint는 통과했다. 운영 코드는 변경하지 않았다.

### 이전 실행 기록

데스크톱 자동 저장은 `pass`다. 실제 `useNoteContentAutosave`, `NotesDataProvider`와 저장 인터페이스를 실행하는 브라우저 명령 모델에서 입력 A 및 B, 799ms와 1ms 및 800ms 진행, `blur`, document hidden, `pagehide`와 저장 조회를 조합했다. 로컬 빠른 프로필의 정상 후보 20개에서 명령 위치 95개를 확인해 81개를 실행하고, 저장 뒤에만 가능한 조회 14개를 거절했다. 시간 상한으로 중단되거나 속성 검사가 생략된 후보는 없다.

- 행동 생성: 각 후보는 메모와 Provider, 저장 인터페이스, DOM과 가상 시계를 새로 만들고 종료 뒤 타이머와 DOM을 정리한다. 입력 뒤 경과 시간은 독립 모델에서 계산하며 실제 저장값을 기대값에 복사하지 않는다. 기존 고정 검사는 A 저장 진행 중 B 입력, 중복 요청 거부, A 완료 뒤 B 저장과 실패 후 재시도를 계속 확인한다.
- 오류 탐지: 저장 인터페이스가 저장 성공을 반환하면서 실제 원문을 갱신하지 않는 결함을 같은 명령과 관찰식으로 실행했다. `autosave-persists-latest-input`에서 예상 원문 `입력 A`와 관찰 원문 `저장된 메모`의 차이를 탐지했다. 실제 저장 조회는 `blur`나 화면 이탈로 보완하기 전에 수행했다.
- 실패 순서 축소: 최초 실패의 `advance(800), advance(800), edit-a, advance(800), advance(800), advance(800), inspect-saved`에서 앞선 시간 진행과 반복 시간을 제거해 `edit-a, advance(800), inspect-saved`를 남겼다. 네 번 축소됐으며 입력과 타이머 및 저장 조회가 모두 남아 같은 위반임을 확인했다.
- 로그 기록: 통제 결함은 탐색에서 명령 위치 68개를 확인해 54개를 실행하고 14개를 거절했다. 축소에서는 49개를 확인해 35개를 실행하고 14개를 거절했다. seed `3`, path `14:3:2:4:3`, replayPath `GBR:F`, 초기 입력과 저장 원문, 예상 및 관찰 원문, 앱과 모델 revision, fast-check 및 Vitest 버전, 실제 프로필, 브라우저 환경, 소요 시간과 종료 이유를 JSON으로 남겼다.
- 재현: seed, path와 replayPath를 사용한 재실행에서 같은 불변 조건이 실패했다. 난수 없이 축소된 세 행동을 실행해 통제 결함은 같은 불변 조건에서 실패하고 정상 저장은 통과했다. 이 결과는 Chromium, Firefox와 WebKit에서 일치했다.

실제 애플리케이션 검사는 앱 코드가 타이머를 만들기 전에 Playwright Clock을 설치했다. 입력 뒤 700ms에는 IndexedDB의 이전 원문을 확인하고 총 800ms 뒤 새 원문을 확인했다. 이어 `blur`와 사용 빈도 화면으로의 내부 이동이 각각 최신 원문을 즉시 저장하는지 같은 저장소에서 읽었다. 새 검사는 Chromium, Firefox와 WebKit에서 통과했다. 799ms를 한 번에 진행한 첫 검사는 Chromium Clock이 800ms 타이머를 실행해 700ms와 나머지 100ms로 바꿨으며, 브라우저 명령 모델은 799ms와 1ms를 별도로 유지한다.

이번 단위는 브라우저 통합 검사, E2E와 공통 E2E 저장 조회 도우미만 변경했다. 운영 화면, 자동 저장 구현, 메모 자료 형식과 오류 처리는 바꾸지 않았다. CI 재현은 원격 저장소와 CI 서비스가 정해진 뒤 별도로 확인한다.

## 모바일 명시적 저장과 이탈 실행 결과

2026년 9월 24일 새 절차의 로컬 판정은 `pass`다. 실제 320px 목록에서 메모를 만들고 상세 화면에서 서로 다른 두 원문을 연속 저장한 뒤, 추가 편집, 저장, 이탈 시도, 계속 편집, 버리기와 재진입을 생성 명령으로 조합했다. 각 후보는 독립 BrowserContext와 IndexedDB를 사용했다. 기대 모델은 현재 입력, 마지막으로 명시적으로 저장한 원문과 화면 위치만 유지하고, 결과는 실제 textarea 및 새 IndexedDB 연결로 확인했다. 정상 명령과 저장 버튼 click 누락을 주입한 명령의 Chromium, Firefox 및 WebKit 실행 여섯 건이 통과했다.

두 번째 저장에서 화면 입력은 바뀌었는데 저장값은 첫 입력으로 남는 실패를 발견했다. 저장 성공 후 폼 `reset`이 필드 참조를 지우고, 유지되는 textarea callback ref가 다시 호출되지 않아 다음 변경을 폼에서 읽을 수 없는 문제였다. 저장 성공의 두 `reset` 분기에서 기존 필드 참조를 유지하도록 수정한 뒤 연속 저장과 이탈 선택을 다시 실행했다. 테스트 준비 단계의 임시 진단 출력은 제거했다.

저장 버튼의 click 연결만 격리해서 누락하자 같은 원문 비교가 실패했다. fast-check는 편집과 저장 두 행동으로 실패를 줄였고, seed, path, replayPath와 축소된 행동의 직접 실행으로 누락 조건에서만 실패하고 정상 저장에서는 통과함을 확인했다. 원문, 브라우저, 최초 및 축소 행동과 재현 정보를 Playwright 첨부 자료에 기록했다. 이는 저장 버튼 연결이 누락된 통제 조건의 탐지 결과이지 실제 사용자의 저장 실패 발생 기록은 아니다.

복구 초안이 있는 모바일 목록에서 상세 화면으로 이동하면 저장 전 원문이 바뀌는 별도 실패도 발견했다. CSS로 숨겨진 데스크톱 보드가 동시에 마운트되어 종료 시 자동 저장한 것이 원인이었다. 실제 메모 작업 영역 너비에 따라 모바일 목록 또는 데스크톱 보드 중 하나만 마운트하도록 변경했다. 복구 원문은 상세 입력에 보이되, 명시적으로 저장하기 전에는 저장된 원문과 content revision이 유지되고 초안도 남는 것을 새 연결로 확인했다. 버리기 뒤 초안 부재, 넓은 화면으로 전환해도 저장 geometry가 유지되는 결과를 포함한 대상 화면 검사 15건이 세 브라우저에서 통과했다. 이 절차에 직접 해당하는 중복 모바일 명령과 검사 한 건을 `notes-model.spec.ts`에서 제거했고, 나머지 사용자 과업 12건도 세 브라우저에서 통과했다.

단위 검사 199건, Browser Mode 48건, 전체 Playwright E2E 190건, 타입 검사와 lint, 운영 빌드가 통과했다. 실제 기기의 입력기 조합 및 OS 탭 전환은 확인하지 않았다. CI 재현은 이번 작업에 포함하지 않는다.

### 이전 실행 기록

모바일 명시적 저장과 이탈은 `pass`다. 320 x 720 viewport의 실제 `NoteDetailPage`에서 원문 편집, 명시적 저장, 저장값 조회, 이탈 시도, 계속 편집, 변경사항 버리기, 목록 재진입과 저장값 재확인을 `fc.commands`로 조합했다. 각 후보는 별도 BrowserContext와 IndexedDB를 사용했다. 정상 후보 20개는 세 브라우저에서 모두 통과했다.

- 행동 생성: 정상 탐색은 명령 위치 90개를 확인해 33개를 실행하고 실행 조건에 맞지 않는 57개를 거절했다. 실제 실행은 저장 4회, 두 편집 17회, 저장 조회 2회, 이탈 시도 5회, 계속 편집 2회와 변경사항 버리기 3회였다. 생성 순서에서 실행되지 않은 목록 재진입은 편집, 이탈 시도, 계속 편집, 다시 이탈, 버리기, 재진입과 조회의 필수 고정 순서로 별도 실행했다. 이 순서의 일곱 행동은 모두 한 번 실행됐고 거절은 없었다.
- 오류 탐지: 격리된 결함 실행은 실제 저장 버튼의 click을 capture 단계에서 중단해 운영 화면이나 저장 코드를 바꾸지 않고 저장 연결 누락을 만들었다. 같은 저장 조회는 예상 원문 `모바일 입력 A`와 IndexedDB 관찰 원문 `모바일 저장 기준 원문`의 차이를 `mobile-save-and-leave-choice-preserve-content`에서 탐지했다. textarea 값과 viewport도 같은 관찰식에서 비교했다.
- 실패 순서 축소: 최초 실패 `edit-a, save, edit-a, edit-b, inspect-stored`에서 저장 뒤의 불필요한 두 편집을 제거해 `edit-a, save, inspect-stored`를 남겼다. 한 번 축소됐고 상세 화면, 입력, 명시적 저장과 저장 조회가 같은 후보 안에 남았다.
- 로그 기록: 통제 결함의 탐색은 명령 위치 74개 중 36개를 실행하고 38개를 거절했으며, 축소는 22개 중 12개를 실행하고 10개를 거절했다. 명령 이름별 수치, seed `5`, path `15:6`, replayPath `AAAABBD:VB`, 초기 화면 및 원문, 예상 및 관찰 원문, 320 x 720 viewport, 앱과 모델 revision, 도구 버전, 브라우저, 실행 시간과 종료 이유를 JSON으로 남겼다.
- 재현: seed, path와 replayPath 재실행 및 난수 없는 세 행동 실행에서 같은 불변 조건이 실패했다. 같은 세 행동은 정상 저장에서 통과했다. Chromium, Firefox와 WebKit에서 같은 최초 및 최소 행동과 관찰 차이를 확인했다.

기존 320px 화면 검사는 명시적 저장 뒤 재진입 값, 계속 편집의 입력 및 포커스 유지, 변경사항 버리기의 저장값 유지와 이탈 완료를 계속 확인한다. 너비를 1280px로 바꾼 뒤에도 모바일 편집 원문과 저장 geometry 전체가 유지되는 비교를 추가했다. 저장 중 대화상자 닫기 차단과 상단 탐색 이탈 검사는 유지했다.

공통 계측은 이제 탐색 및 축소 합계뿐 아니라 명령 이름별 시도, 실행과 거절을 함께 저장한다. 생성, 본문 revision과 자동 저장 검사도 같은 자료 구조를 사용하므로 다음 실행부터 이름별 수치가 기록된다. 운영 UI, 모바일 저장 구현, 메모 자료 형식과 오류 처리는 바꾸지 않았다. CI 재현은 원격 저장소와 CI 서비스가 정해진 뒤 별도로 확인한다.

## 초안 복구와 제거 재시도 실행 결과

초안 복구 검증 절차는 완료했지만 서비스 판정은 `needs revision`이다. 복구 판정과 저장 명령은 행동 생성, 통제 결함 탐지, 행동 순서 축소, 구조화 로그와 두 방식의 재현을 충족했다. 새 브라우저 페이지에서도 복구, 중간 초안, 제거 재시도와 오래된 초안 거부를 확인했다. 다만 320px 목록에서 상세 화면으로 이동하면 명시적 저장 전에 복구 원문이 메모에 반영되는 동작을 별도로 발견했다.

- 행동 생성: 초안 판정은 복구 가능, 같은 원문, 오래된 revision과 다른 메모 ID의 초안 준비, 복구 시도 및 입력 조회를 조합했다. seed `11`의 후보 100개에서 457개 행동을 모두 실행했다. 저장 명령은 편집, 저장 요청, 저장 중 요청, 완료, 초안 및 메모 저장 실패와 조회를 생성했다. seed `12`의 후보 100개에서 명령 위치 462개를 확인해 338개를 실행하고 상태에 맞지 않는 124개를 거절했다.
- 오류 탐지: 첫 통제 결함은 메모 저장이 실패한 뒤 복구 초안을 없애 `draft-state-matches-save-outcome`의 예상 초안과 부재 상태를 비교했다. 두 번째 결함은 메모 저장은 성공하고 초안 제거만 실패한 상태를 저장 상태에 전달하지 않아, 같은 원문의 다음 저장 요청이 사라진 차이를 `draft-cleanup-retried-on-next-save`에서 탐지했다.
- 실패 순서 축소: 초안 손실은 `fail-next-note-save, edit("A"), request-save, inspect-recovery, complete-save`에서 조회 하나를 제거한 네 행동으로 줄었다. 제거 재시도 누락은 같은 편집 두 번이 든 여섯 행동에서 하나를 제거해 `fail-next-draft-remove, edit("A"), request-save, complete-save, request-save`를 남겼다. 두 사례 모두 한 번 축소됐고 실패 작업, 저장 완료와 관찰 시점이 유지됐다.
- 로그 기록: 초안 손실은 탐색 434개 중 359개 실행 및 75개 거절, 축소 37개 중 27개 실행 및 10개 거절, seed `10`, path `80:8`, replayPath `AADDD:V`를 기록했다. 제거 재시도 누락은 탐색 141개 중 117개 실행 및 24개 거절, 축소 46개 중 41개 실행 및 5개 거절, seed `8`, path `28:6`, replayPath `BBDAE:V`를 기록했다. 두 로그에는 초기 메모와 초안, 예상 및 관찰 상태, 불변 조건, 앱과 모델 revision, 도구 버전, 프로필, 실행 시간과 종료 이유가 포함된다.
- 재현: 각 결함은 seed, path와 replayPath로 같은 불변 조건을 다시 실패시켰다. 난수 없이 최소 행동을 실행해도 같은 결함은 실패하고 정상 저장은 통과했다. 320 x 720의 새 페이지는 기존 메모와 복구 초안이 있는 같은 BrowserContext에서 상세 주소를 직접 열었다. 세 브라우저 모두 복구 원문을 표시하고 새 입력을 저장했으며, 메모 저장 뒤 초안 제거만 막았을 때 메모와 초안이 함께 남았다. 제거 차단을 해제한 다음 같은 원문을 다시 저장하면 초안만 사라지고 메모 revision은 유지됐다. 오래된 revision의 초안을 다시 넣고 페이지를 새로 열었을 때에는 저장된 원문을 표시했다.

일반 모바일 목록에서 상세 화면으로 이동하는 실행에서는 CSS로 숨겨진 `NotesBoard`가 계속 마운트돼 있었다. 상세 화면으로 이동할 때 이 보드가 언마운트되면서 복구 원문을 자동 저장했고, 상세 화면의 저장 버튼을 누르기 전에 content revision이 한 번 증가했다. 모바일의 명시적 저장 원칙과 맞지 않으므로 직접 주소를 연 통과 사례로 이 동작을 덮지 않는다. 운영 UI와 저장 코드는 이번 절차에서 바꾸지 않았으며, 숨은 데스크톱 편집기의 저장 수명을 모바일 화면과 분리한 뒤 목록에서 상세 화면으로 이동하는 과정을 다시 확인해야 한다.

## 위치와 크기 및 속성 적용 실행 결과

위치와 크기 및 속성 적용은 `pass`다. 1280 x 900의 실제 메모 카드, 속성 패널과 IndexedDB를 실행하는 명령 모델에서 허용 좌표 적용, 헤더 이동, 남동쪽 크기 조절, pointer capture 상실에 따른 이동 취소, 잘못된 너비의 안내 및 수정과 저장값 확인을 조합했다. 각 후보는 별도 BrowserContext와 저장소를 사용했으며 Chromium, Firefox와 WebKit에서 같은 결과를 확인했다.

- 행동 생성: 정상 탐색은 브라우저마다 명령 위치 74개를 확인해 24개를 실행하고 상태에 맞지 않는 50개를 거절했다. 실행된 행동은 허용 X 좌표 적용 4회, 헤더 이동 2회, 남동쪽 크기 조절 6회, 취소 5회, 잘못된 너비 안내 및 수정 2회와 저장값 확인 5회다. 생성된 `apply-x(500)`은 seed `13`에서 실행되지 않아 필수 순서에서 X `5000` 저장을 별도로 확인했다. 기존 실제 화면 검사의 X `-500` 저장도 유지했다.
- 오류 탐지: 통제 결함은 실제 메모 화면에서 pointer 종료 전달만 중단했다. 모델은 mouse, 배율 1, 헤더 시작점과 `80:40` 이동량으로 X `112`, Y `72`, 전체 revision `2`를 예상했지만 IndexedDB에는 X `32`, Y `32`, 전체 revision `1`이 남았다. 원문과 content revision `1`, 너비, 높이와 `zIndex`는 같은 관찰식에서 비교했다. 불변 조건 `requested-geometry-is-stored-without-content-revision-change`가 적용 무시를 탐지했으며 운영 코드에는 결함 분기를 넣지 않았다.
- 실패 순서 축소: 최초 실패 `inspect-geometry-baseline, pointer-move(...), inspect-stored-geometry`에서 앞의 기준값 확인을 제거해 이동과 저장값 확인만 남겼다. 한 번 축소됐으며 최소 행동 문자열은 pointer 종류, 배율, 시작 위치, 이동량과 `pointerup` 종료를 보존한다.
- 로그 기록: 통제 결함의 탐색 단계는 3개 행동을 모두 실행했고 축소 단계는 2개 행동을 모두 실행했다. feature와 실행 계층, 초기 원문, viewport, pointer 및 배율, 최초 및 최소 행동, 기대 및 관찰 geometry와 두 revision, 앱과 모델 revision, 도구 버전, 프로필, 실행 시간과 종료 이유를 JSON으로 남겼다. 세 브라우저 모두 seed `15`, path `0:0`, replayPath `null`이며 배열 축소에는 별도 replayPath가 필요하지 않았다.
- 재현: seed와 path로 다시 실행해 같은 불변 조건 실패를 확인했다. 난수 없이 이동과 저장값 확인을 실행했을 때 pointer 종료를 막은 실행은 실패하고 정상 화면은 통과했다. 필수 순서에서는 너비 `4096`의 오류 안내 뒤 기존 너비로 수정, X `5000` 저장, 이동 취소와 남동쪽 `48:32` 크기 조절을 각각 저장값 확인과 연결했다.

전체 geometry를 비교하므로 남동쪽 크기 조절에서 X와 Y가 유지되고 원문과 content revision 및 `zIndex`가 바뀌지 않는 조건도 함께 확인한다. 기존 속성 검사는 허용 범위, 유한하지 않은 좌표 거절과 새 메모 배치를 계속 확인하고, 고정 E2E는 음수 좌표, 속성 패널의 수정 안내, 최신 이동 위치, 실제 drag 및 resize와 빈 캔버스 보기 이동을 확인한다. 이번 절차는 운영 UI, 저장 코드와 메모 자료 형식을 바꾸지 않았다. CI 재현은 원격 저장소와 CI 서비스가 정해진 뒤 별도로 확인한다.

## 겹침 순서 실행 결과

겹침 순서는 `pass`다. 1280 x 900의 실제 메모 카드 세 개와 IndexedDB를 실행하는 명령 모델에서 맨 앞 이동, 맨 뒤 이동, 중복 `zIndex` 준비, 새로고침과 저장 및 화면 확인을 조합했다. 각 후보는 별도 BrowserContext와 저장소를 사용했으며 세 브라우저에서 같은 행동 수와 축소 결과를 확인했다.

- 행동 생성: 정상 탐색은 브라우저마다 명령 위치 105개를 확인해 60개를 실행하고 현재 상태에서 의미가 없는 45개를 거절했다. 맨 앞 이동은 10회, 맨 뒤 이동은 18회, 중복 순서 준비는 9회, 새로고침은 13회, 저장 및 화면 확인은 10회 실행됐다. 위치 번호는 현재 뒤에서 앞으로 나열한 ID에서 대상을 고르며 모델은 다른 메모의 상대 순서만 계산한다.
- 오류 탐지: 첫 통제 결함은 실제 카드의 맨 앞 및 맨 뒤 버튼 click 전달을 막았다. 첫 메모를 맨 앞으로 보낸 모델은 `zIndex`를 `3`으로 예상했지만 IndexedDB와 계산된 화면 스타일은 `1`로 남았다. 두 번째 통제 결함은 한 transaction의 두 번째 메모만 변경 전 번호로 저장해 화면의 `2`와 IndexedDB의 `3`이 다른 부분 저장을 만들었다. 불변 조건 `stack-move-persists-one-to-n-with-stable-tab-and-content-revision`은 ID별 `1..N`, 저장값과 화면값, `tabIndex`와 content revision을 함께 비교해 두 결함을 탐지했다.
- 실패 순서 축소: 두 결함 모두 최초 실패의 기준값 확인 네 번, `move-front(0)`, 저장 및 화면 확인에서 앞의 네 행동을 제거했다. 최소 행동은 `move-front(0), inspect-stored-stack`이며 각각 한 번 축소됐다. 메모 세 개의 ID와 예상 및 관찰 번호는 초기 자료와 실패 값에 남겼다.
- 로그 기록: 각 통제 결함의 탐색은 6개 행동을 모두 실행했고 축소는 2개 행동을 모두 실행했다. feature와 실행 계층, 초기 원문 세 개 및 `zIndex`, 최초 및 최소 행동, ID별 기대 및 관찰 `zIndex`, `tabIndex`와 content revision, 앱과 모델 revision, 브라우저, 도구 버전, 프로필, 실행 시간과 종료 이유를 JSON으로 남겼다. 이동 무시는 seed `17`, 부분 저장은 seed `18`이며 세 브라우저 모두 path `0:0`, replayPath `null`이다.
- 재현: 두 결함 모두 seed와 path 재실행 및 난수 없는 두 행동에서 같은 불변 조건이 실패했고 정상 화면은 통과했다. 필수 순서는 실제 IndexedDB의 세 메모를 모두 `zIndex=2`로 만든 뒤 두 번째 메모를 맨 앞으로 보내고 새로고침했다. 이어 현재 앞의 메모를 맨 뒤로 보내 다시 확인했으며, 매번 저장소와 화면에 `1..3`이 있고 다른 메모의 상대 순서, `tabIndex`와 content revision이 유지됐다.

명령 모델은 운영 정렬 함수나 전체 revision 증가 방식을 기대값 계산에 사용하지 않는다. 전체 revision의 정확한 증가 횟수는 이 요구사항에 없으므로 검사 대상에서 제외했다. 실제 화면 동작은 Provider의 `saveAll`을 거쳐 한 IndexedDB readwrite transaction에 세 메모를 저장하며, 새로고침 뒤 다시 읽은 값까지 확인했다. 기존 저장소 검사는 삭제 및 복원으로 같은 `zIndex`가 생긴 상태와 다음 명시적 이동도 새 연결에서 계속 확인한다. 이번 절차는 운영 UI와 순서 구현을 바꾸지 않았다.

## 삭제와 취소 및 만료 실행 결과

삭제와 취소 및 만료는 `pass`다. 1280 x 900의 실제 메모 카드 세 개, 실행 중 삭제 이력과 IndexedDB를 사용하는 명령 모델에서 삭제, 맨 앞으로 이동, 최근 삭제 복원, 시간 진행, route 왕복, 새로고침과 저장 및 화면 확인을 조합했다. 각 후보는 별도 BrowserContext와 저장소를 사용하고 앱의 timer가 생기기 전에 Playwright Clock을 설치했으며 Chromium, Firefox와 WebKit에서 같은 결과를 확인했다.

- 행동 생성: 정상 탐색과 별도 필수 순서를 함께 실행한다. 정상 후보 10개는 브라우저마다 명령 위치 52개를 확인해 23개를 실행하고 현재 이력이나 메모 상태에 맞지 않는 29개를 거절했다. 후보별 삭제, 순서 이동, 복원, 시간 진행, route 왕복, 새로고침과 저장 및 화면 확인을 임의 순서로 조합한다. 생성에서 실행되지 않을 수 있는 만료 직전 시간은 필수 순서로 보완했다. 별도 필수 순서는 순서 변경 뒤 복원 및 중복 번호 정리, 삭제 완료 시각 기준 1ms 전과 만료 시점, 연속 삭제 두 건의 LIFO 복원과 새로고침 뒤 이력 부재를 포함해 22개 행동을 실행했다.
- 오류 탐지: 통제 결함은 순서 변경이 있었을 때 복원 대상의 IndexedDB `zIndex`만 1 높여 저장했다. 모델과 화면은 삭제 전 값 `2`를 예상했지만 저장소에서만 `3`을 읽었다. 불변 조건 `removal-restores-snapshot-focus-and-absolute-expiry`는 ID별 원문, content revision, geometry, `tabIndex`, 계산된 화면 순서, 토스트와 포커스를 함께 비교해 차이를 탐지했다. 복원 명령은 대상 ID가 다시 저장될 때까지 기다리고, 원문, 전체 revision, content revision, 위치, 크기와 Tab 순서가 삭제 전 스냅샷과 같은지도 별도로 확인한다.
- 실패 순서 축소: 최초 실패 `inspect-removal-baseline, remove(1), move-front(0), restore-latest, inspect-removal-state`에서 기준값 확인을 제거했다. 최소 행동은 `remove(1), move-front(0), restore-latest, inspect-removal-state`이며 한 번 축소됐다. 결함은 순서 변경 뒤에만 작동하므로 삭제와 다른 메모의 순서 변경을 제거하면 더 이상 실패하지 않는다.
- 로그 기록: 통제 결함의 탐색 단계는 5개 행동을 실행하고 축소 단계는 4개 행동을 실행했다. feature와 실행 계층, 기준 시각, 초기 원문 및 `zIndex`, 최초 및 최소 행동, ID별 기대 및 관찰 저장값과 화면값, 포커스, 토스트, 앱과 모델 revision, 브라우저, 도구 버전, 프로필, 실행 시간과 종료 이유를 JSON으로 남겼다. 세 브라우저 모두 seed `20`, path `0:0`, replayPath `null`이다.
- 재현: seed와 path 재실행 및 난수 없는 네 행동에서 같은 불변 조건이 실패했고 정상 화면은 통과했다. 필수 순서에서는 `A=1, B=2, C=3`에서 B 삭제, A 맨 앞으로, B 복원 뒤 `C=1, A=2, B=2`를 저장소와 화면에서 확인하고 B의 명시적 맨 앞으로 이동 뒤 `1..3`으로 정리되는 것도 확인했다. 삭제 알림은 route 왕복 뒤에도 원래 삭제 완료 시각의 1ms 전에는 남고 만료 시점에는 사라졌으며, 새로고침 뒤에는 삭제된 자료를 복원하지 않고 이력만 사라졌다.

Clock을 설치한 뒤 로딩과 입력에 걸린 실제 시간을 기대 모델에서 빠뜨리면 만료 직전 상태를 잘못 실패로 판정했다. 로딩 전 더 이른 시각에 Clock을 설치하고 초기 자료 준비 뒤 기준 시각에 일시 정지했으며, 포커스 이동용 animation frame도 모델 시각에 포함했다. 이 보정 뒤 세 브라우저가 같은 절대 만료 결과를 냈다. 기존 `note-removal-history.test.ts`는 복원을 시작한 스냅샷 뒤 새 삭제가 추가돼도 시작한 스냅샷만 제거하는 ID와 제거 시각 비교를 계속 확인한다. 이번 절차는 운영 UI, 삭제 및 복원 구현과 자료 형식을 바꾸지 않았다.

전체 Playwright 검사 123개는 세 worker로 실행해 모두 통과했다. 여섯 worker에서는 모델 탐색 시간이 고정된 30초를 넘거나 초기 화면의 미리 불러오기 요청이 실패했지만, 세 worker에서는 같은 행동 수를 모두 실행하고 정적 자산 오류도 발생하지 않았다. 모바일 저장 및 겹침 순서 모델의 탐색 상한은 60초, 테스트 상한은 180초로 늘렸고 전체 실행은 세 worker로 제한했다. 초기 화면 오류 검사는 관찰 도중 바뀌는 배열을 두 번 읽지 않도록 한 시점의 복사본을 비교하며 루트 주소의 알려진 WebKit 미리 불러오기 오류도 다른 경로와 같은 규칙으로 분류한다. CI 재현은 원격 저장소와 CI 서비스가 정해진 뒤 별도로 확인한다.

## 선택과 포커스 및 속성 대상 실행 결과

선택과 포커스 및 속성 대상은 `pass`다. 1280 x 900의 실제 메모 카드 두 개, 선택 테두리, 속성 패널과 브라우저 활성 요소를 사용하는 명령 모델에서 `Tab`, 헤더 click 및 double click, 메모 선택 지점과 자식 제어의 `Enter`, `Escape`, `Command`, 빈 캔버스와 패널 닫기를 조합했다. 각 후보는 별도 BrowserContext와 IndexedDB를 사용했으며 Chromium, Firefox와 WebKit에서 같은 판정을 확인했다.

- 행동 생성: 정상 후보 10개는 브라우저마다 명령 위치 51개를 확인해 32개를 실행하고 현재 선택이나 패널 상태에 맞지 않는 19개를 거절했다. 명시적 조회가 생성되지 않은 후보도 통과하지 않도록 각 후보가 끝날 때 조건 없는 최종 관찰을 추가했다. 선택은 전체 메모를 감싸는 계산된 테두리, 속성 대상은 패널 제목, 유효하지 않은 너비 초안은 입력값, 포커스는 `document.activeElement`와 실제 메모 ID를 대조해 관찰한다. 필수 순서 28개는 생성 순서에서 빠질 수 있는 선택 지점 `Enter`, 패널 닫기, 자식 편집기 및 헤더 버튼 `Enter`, 세 선택 해제 방식을 보완했다.
- 오류 탐지: 실제 빈 캔버스 pointer 입력이 끝난 뒤 B 헤더의 double click이 잘못 전달되는 통제 결함을 테스트 환경에만 설치했다. 모델은 선택 없음, A 패널 제목과 너비 초안 `4096`, 캔버스 포커스를 예상했지만 화면에는 B 패널 제목, 너비 `320`과 B 선택 테두리가 나타났다. 불변 조건 `selection-focus-and-properties-target-stay-independent`가 선택, 포커스, 패널 대상과 초안의 차이를 한 번에 탐지했다.
- 실패 순서 축소: 최초 실패의 기준 상태 조회 네 번, A 패널 열기, 잘못된 너비 입력, B 선택, 빈 캔버스 선택 해제와 조회에서 앞의 조회 네 번을 제거했다. 최소 행동은 `open-properties-pointer(0), edit-width(4096), select-header-click(1), clear-selection(blank), inspect-selection-state`이며 한 번 축소됐다. A와 B를 하나로 줄이면 패널 대상과 현재 선택이 바뀐 문제를 구별할 수 없으므로 두 메모는 초기 자료에 남겼다.
- 로그 기록: 통제 결함의 탐색 단계는 9개 행동을 실행했고 축소 단계는 5개 행동을 실행했다. feature와 실행 계층, 초기 메모와 포커스, 최초 및 최소 행동, 기대 및 관찰 포커스, 헤더 표시 여부, 패널 제목과 너비, 메모별 선택 표시, 앱과 모델 revision, 브라우저, 도구 버전, 프로필, 실행 시간과 종료 이유를 JSON으로 남겼다. 세 브라우저 모두 seed `24`, path `0:0`, replayPath `null`이다.
- 재현: seed와 path로 다시 실행하고 난수 없이 최소 행동을 실행해 같은 불변 조건 실패를 확인했다. 같은 다섯 행동은 정상 화면에서 통과했다. 실제 입력의 필수 순서에서는 `Tab`과 한 번 click이 선택만 바꾸고 패널을 열지 않았으며, 선택 지점의 보조 키 없는 `Enter`는 패널을 열고 X 입력으로 포커스를 옮겼다. 선택하지 않은 지점, textarea와 헤더 버튼의 `Enter`는 패널을 열지 않았다. `Escape`, `Command`와 빈 캔버스는 선택을 없애면서 A 패널 제목과 `4096` 초안을 유지했고, `Command` 중 숨었던 헤더는 keyup 뒤 다시 나타났다.

기존 `note-workspace-state.test.ts`는 선택 ID, 속성 대상, 최근 패널, 포커스 요청과 늦게 끝난 저장의 순수 상태 전이를 빠르게 확인한다. 기존 `notes.spec.ts`는 저장된 `tabIndex` 순서, 패널 입력 적용, 한 번 click과 double click, 선택 해제 및 패널 전환의 고정 사례를 확인한다. 새 명령 모델은 이 두 검사를 목록으로 복사하지 않고, 서로 다른 두 메모에서 행동 순서를 섞고 같은 화면 관찰식으로 통제 결함을 줄이는 책임만 추가한다.

통제 결함을 만들 때 사용한 합성 double click은 탐지기가 잘못된 연결을 구별하는지 확인하는 용도이며 정상 동작의 입력 증거로 세지 않는다. 정상 및 직접 재현은 Playwright가 보낸 pointer와 keyboard 입력을 사용했다. Playwright는 운영체제 입력기 조합을 직접 구동하지 않으므로 실제 IME 조합 중 `Escape` 동작은 이번 완료 근거에 포함하지 않는다.

운영 빌드와 전체 Playwright 126개, 단위 검사 187개, 타입 검사 및 lint가 통과했다. 모든 후보의 최종 관찰을 추가한 뒤 선택 모델 3개를 세 브라우저에서 다시 실행해 통과했다. 운영 UI, 선택 및 패널 상태 코드, 메모 자료 형식과 요구사항은 바꾸지 않았다. CI 재현은 원격 저장소와 CI 서비스가 정해진 뒤 별도로 확인한다.

## 기능별 입증 계획과 기존 작업 유지 판단

2026년 9월 13일에 [기능별 조사](references/note-feature-exploration-research.md)와 [계획](plan.md#u12-모델-기반-탐색과-실패-재현)을 정리했다. 생성, 본문 편집, 자동 저장, 모바일 저장, 초안 복구, 위치 및 크기, 겹침 순서, 삭제 취소, 선택 및 포커스, 캔버스 보기, 개별 복사와 사용 기록으로 나눴다. 각 기능에 행동 생성, 오류 탐지, 실패 순서 축소, 로그 기록과 재현 상태를 표시하고 필요한 실제 저장 및 화면 증거를 함께 완료하도록 바꿨다.

공통 실행 방안에는 후보별 초기화, 실제 요청과 기대값의 분리, 명령별 실행량, 일반 실패 로그, seed 및 직접 행동 재현을 포함했다. 시간 제한이 진행 중 Promise를 취소하지 않는 점, 예산 중단과 정상 완료의 구분, 직접 만든 BrowserContext의 trace와 불변 조건 로그도 정리했다. 기능마다 필요한 결함 후보와 축소에서 보존할 선행 행동을 기록했으며 새 결함을 실행해 확인한 결과는 아니다.

당일 변경분의 제거 필요성은 읽기 조사로 확인했다. 초안 제거 재시도 운영 수정, fast-check와 실행 설정, 원문 및 revision 비교, 부분 실패 검사와 실제 IndexedDB 재조회는 유지할 근거가 있다. 기존 코드를 통째로 되돌릴 이유는 확인하지 못했다. 모델 연결, 일반 실패 보고와 화면 관찰은 목적을 보존해 보완할 대상이며 중복 검사의 제거 여부는 대체 증거 확보 뒤 판단한다. 상세 근거는 [제거 필요성 조사](references/note-feature-exploration-research.md#기존-작업의-제거-필요성)에 있다.

이번 변경은 요구사항의 기능별 완료 조건, 조사, 계획 및 문서 간 참조의 갱신이다. 기존 단계 1부터 4의 실행 기록은 아래에 남기되 현재 기능 완료 판정으로 사용하지 않는다. 운영 코드, 테스트, 의존성과 Git 이력을 제거하거나 수정하지 않았고 테스트도 재실행하지 않았다. CI 서비스와 원격 저장소 선택은 여전히 미정이다.

## 후속 조사에 따른 판정 정정

2026년 9월 13일의 [후속 조사](references/model-testing-validity-research.md#보완-후에도-남은-문제와-근본-원인)는 실패 보고, 계측, 모델 독립성, 축소, 비동기 저장, 화면 조작과 문서 상태를 비교했다. 근본 원인은 요구사항마다 구별해야 할 오류와 관찰 시점을 실행 증거에 연결하지 않은 채 일부 통과를 단계 전체 완료로 해석한 것이다. 아래 단계별 `pass`는 당시의 제한된 검사 판정이며 현재 단계 완료를 뜻하지 않는다.

기존 기록의 생략 0개는 fast-check의 속성 검사 `numSkips=0`이다. 명령의 `check`가 거절한 횟수를 측정한 값이 아니므로 명령 생략 수는 미확인이다. 결함의 seed 재실행은 고정된 행동 순서의 입력 축소이며 `fc.commands`의 행동 순서 축소와 replayPath 재현은 아직 증명하지 않았다.

직전 읽기 검토에서 `pnpm --filter notes-app test:model`은 3개 파일, 13개 검사와 각 모델 100개 사례를 통과했다. 명령 실행 총합은 복사 519회, 저장 362회와 생명 주기 381회였다. 저장 및 복사의 결함 입력은 공백 한 글자, 복원 결함 입력은 `[0,0]`으로 축소됐다. 이 통과 결과는 새 서비스 버그 발견이나 전체 브라우저 재실행의 증거가 아니다.

이 후속 조사에서는 운영 코드, 테스트, 의존성과 CI를 변경하거나 새 결함을 주입하지 않았다. 실패 보고 및 계측, 저장 입력 분리, 실제 저장과 조작의 보완점은 현재 [기능별 재보완 절차](plan.md#로컬-검증력-재보완-절차)에 연결되어 있다. 아래 단계 1부터 4의 `pass`와 실행 수치는 당시 기록이며 현재의 기능별 입증 상태는 앞부분과 계획을 따른다.

## 단계 1: 편집, 저장과 복구

독립 모델로 보완했다. `note-content.model.test.ts`의 모델은 현재 입력, 저장된 원문 및 원문 revision, 보관된 초안의 기준 revision과 다음 저장소 실패만 계산한다. `beginNoteContentSave`의 요청과 `saveNoteContent`의 반환값은 모델을 갱신하는 근거가 아니라 비교할 관찰값이다. 모델은 같은 저장이 진행 중일 때 새 요청을 만들지 않는 비정상 시도도 별도 명령으로 실행한다.

명시적 순서는 A 저장 요청 뒤 B 입력과 중복 요청, A 완료, B의 메모 쓰기 실패와 재시도, C 저장 뒤 초안 제거 실패, 같은 원문의 다음 저장에서 초안 제거 재시도까지 연결한다. 저장된 원문과 revision, 초안 내용과 기준 revision, 복구 가능 여부를 같은 관찰식으로 비교한다. 이 검사는 실제 IndexedDB 또는 화면 복구의 증거가 아니며, 실제 저장소와 화면 복구 검사는 단계 4에 남아 있다.

통제된 저장소 결함도 같은 관찰식으로 확인했다. 저장한 메모를 이전 값으로 돌려주는 결함, 메모 저장 실패 뒤 초안을 없애는 결함, 첫 저장 뒤 다음 저장을 이전 값으로 되돌리는 결함은 정상 Adapter가 아닌 경우에만 실패한다. 첫 결함은 fast-check가 입력 `" "`로 축소했고, seed `20260913`, path `0:0:0`으로 재실행한 뒤 같은 입력을 난수 생성 없이 다시 실행해 같은 위반을 확인했다.

`pnpm --filter notes-app test:model`의 `local-fast` 실행은 생성 사례 100개, 실제 명령 350개, 생략 0개를 기록했고 3개 파일의 8개 검사가 통과했다. 같은 변경에서 타입 검사와 lint도 통과했다. 전체 `pnpm notes:test`는 34개 파일, 178개 검사가 통과했다.

단계 1은 `pass`다. [초안 제거 재시도 결정](decisions/note-draft-cleanup-retry.md)에 따라 변경된 메모 저장 뒤 초안 제거만 실패하면 저장 성공을 유지한다. 저장 상태는 초안 정리가 남았음을 보관하고, 다음 저장은 원문이 같아도 제거를 다시 시도한다. 재시도 성공은 메모 원문과 revision을 바꾸지 않으며 초안을 없앤다.

## 단계 2: 생성, 배치, 삭제와 복원

독립 모델로 보완했다. `note-lifecycle.model.test.ts`의 모델은 메모 전체 자료와 제거 시각을 별도로 보관한다. 운영 함수는 모델의 기대값을 만드는 데 사용하지 않는다. 각 명령 뒤에는 원문, 원문 revision, ID, geometry와 Tab 순서를 실제 도메인 결과와 직접 비교한다. 위치와 크기 변경, 맨 앞으로 및 맨 뒤로 이동, 삭제, 최근 삭제 복원, 5초 만료와 빈 이력 복원 시도를 생성한다.

고정 순서는 `A=1, B=2, C=3`에서 B 삭제, A 맨 앞으로, B 복원으로 실행했다. 복원 직후에는 `C=1, A=2, B=2`가 남고, B를 다시 맨 앞으로 보내면 `C=1, A=2, B=3`으로 정리된다. 복원한 메모의 `zIndex`를 1 올리는 통제된 결함은 같은 관찰식에서 실패했다. fast-check는 입력 `[0,0]`으로 축소했고 seed `20260913`, path `0:0:0` 재실행과 난수 없이 같은 입력을 실행한 경우에도 같은 위반을 확인했다.

`pnpm --filter notes-app test:model`은 생성 사례 100개, 실제 생명 주기 명령 369개, 생략 0개를 기록했다. 3개 파일의 10개 검사가 통과했다. 같은 변경에서 `pnpm notes:typecheck`, `pnpm notes:lint`, `pnpm notes:test`가 통과했고 전체 검사는 34개 파일, 180개 검사였다. 단계 2는 `pass`다. 실제 IndexedDB 새 연결과 화면에서 중복 `zIndex`를 보존하는 검사는 단계 4에 남아 있다.

## 단계 3: 개별 복사와 사용 기록

독립 모델로 보완했다. `copy-note.model.test.ts`의 모델은 현재 원문, 원문 revision, 복사한 원문과 사용 기록을 각각 보관한다. 위치 변경은 content revision을 바꾸지 않는 실제 `reviseNote` 결과와 비교한다. 복사 명령은 복사 시점의 원문, revision과 ID를 Clipboard와 사용 기록에 정확히 전달했는지 비교한다. 이후 메모를 편집해도 이전 사용 기록이 새 revision으로 바뀌지 않는지 확인한다.

Clipboard 쓰기 실패는 복사 원문과 사용 기록을 모두 추가하지 않는다. Clipboard가 성공한 뒤 사용 기록만 실패하면 복사 원문은 남고 사용 기록만 빠진다. 복사 당시 revision을 하나 높여 기록하는 통제된 결함은 같은 관찰식에서 실패했다. fast-check는 입력 공백 한 글자로 축소했고 seed `20260913`, path `0:0:0` 재실행과 난수 없이 같은 입력을 실행한 경우에도 같은 위반을 확인했다. 빈 원문을 Clipboard에 쓰는 결함도 직접 실행에서 검출했다.

`pnpm --filter notes-app test:model`은 생성 사례 100개, 실제 개별 복사 명령 495개, 생략 0개를 기록했다. 3개 파일의 13개 검사가 통과했다. 같은 변경에서 `pnpm notes:typecheck`, `pnpm notes:lint`, `pnpm notes:test`가 통과했고 전체 검사는 34개 파일, 183개 검사였다. 단계 3은 `pass`다. 일괄 복사 목록은 포함하지 않았으며, 실제 Clipboard 권한과 사용 빈도 화면은 단계 4에 남아 있다.

## 단계 4: 실제 저장소와 메모 화면 연결

실제 IndexedDB 검사를 보완했다. 세 메모를 저장한 뒤 B를 삭제하고 즉시 조회해 B가 없고 A와 C만 남았음을 확인한다. 이어 A를 명시적으로 맨 앞으로 보낸 뒤 B를 저장하면 새 연결에서 `C=1, A=2, B=2`를 읽는다. 복원으로 생긴 같은 `zIndex`는 그대로 보존한다. B를 다시 명시적으로 맨 앞으로 보낸 뒤 또 다른 새 연결에서 `C=1, A=2, B=3`을 읽는다. ID별로 비교해 IndexedDB의 object store 읽기 순서를 겹침 순서의 기준으로 사용하지 않는다. 연결과 저장 자료는 사례마다 초기화하고 모든 연결을 해제한다. 이 검사는 실제 운영 repository의 고정 순서 검사이며 생성한 행동 순서 검사는 아니다.

`notes-model.spec.ts`에는 데스크톱의 세 메모를 생성하고, B 삭제, A의 명시적 순서 변경, B 취소 복원, 새로고침 순으로 실행하는 사례를 추가했다. 새로고침 뒤 메모 세 개와 각각의 입력 원문을 확인한다. 이 검사는 CSS 숫자나 DOM 순서를 읽지 않고 사용자가 실행하는 버튼, 알림, 입력값과 새로고침 뒤 상태를 관찰한다. 기존 사례는 생성, 편집, 800ms 자동 저장, 삭제와 취소, fast-check 원문 후보의 새 BrowserContext, 320px의 명시적 저장과 새로고침을 계속 확인한다.

`pnpm --filter notes-app test:browser`는 12개 파일, 48개 검사가 통과했다. 변경 직후 E2E 파일은 Chromium, Firefox와 WebKit에서 12개 검사가 통과했고, 전체 `pnpm notes:test:e2e`는 빌드 완료 뒤 99개 검사가 통과했다. `pnpm --filter notes-app test:model`은 3개 파일, 13개 검사와 생성 사례 각 100개를 통과했다. 실제 명령 수는 복사 464개, 원문 저장 368개, 생명 주기 398개였고 생략은 모두 0개였다. `pnpm notes:test`는 34개 파일, 183개 검사, `pnpm notes:typecheck`와 `pnpm notes:lint`도 통과했다.

단계 4는 `pass`다. 이 검사는 실제 브라우저가 강제로 종료되는 상황, 운영체제 수준의 앱 이탈 또는 IndexedDB 자체 장애에서의 자료 보존을 증명하지 않는다. 그런 조건은 현재 제어할 수 없으므로 완료 근거에 포함하지 않는다.

## 검증력 조사와 보완 계획

2026년 9월 13일에 기대값, 결과 관찰, 행동 실행량, 비동기 및 저장, 완료 증거를 조사했다. [원인과 공식 자료](references/model-testing-validity-research.md)를 정리하고 [U12 보완 순서](plan.md#보완-순서와-단계별-완료-증거)를 추가했다. 테스트와 운영 코드, 의존성 및 CI는 변경하지 않았다. 결함 주입과 테스트 재실행도 이번 문서 작업의 결과로 보고하지 않는다.

현재 U12 전체 상태는 `needs revision`이다. 단계 1부터 단계 4까지 일부 증거를 보완했으나 후속 조사에서 확인한 로컬 미완료 조건과 CI 재현이 남아 있다. 완료 판정은 문서 앞부분의 정정과 플랜의 재보완 절차를 따른다.

## 남은 판정과 실행 환경

다음 작업은 로컬 재보완이다. 로컬 완료 증거를 확보한 뒤 CI 서비스와 실행할 원격 저장소가 정해지면 같은 잠금 파일과 도구 버전으로 재현한다.

삭제 전 숫자 보존에 관한 추가 조사에서는 현재 복원과 일반 저장이 이미 숫자를 보존하며, 화면 동률 처리와 명시적 이동의 동률 처리 기준은 다를 수 있음을 확인했다. [적용 조사](references/note-restoration-z-index-research.md)에 코드 근거와 표준을 기록하고 요구사항, 삭제 복원 결정과 플랜을 업데이트했다. 실제 브라우저 실행이나 서비스 코드 수정의 완료 기록은 아니다.

CI 재현은 Git remote와 CI 서비스가 정해지지 않아 시작하지 않았다. 로컬 실행 기록을 특정 서비스의 workflow나 원격 저장소 설정으로 바꾸지 않았다.
