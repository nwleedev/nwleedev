## 상세

이 애플리케이션은 단순한 웹 애플리케이션이 아닙니다.

프론트엔드 역량을 보여주기 위한 다양한 미니 애플리케이션을 제공하는 포트폴리오 애플리케이션입니다.

사용자가 웹 사이트에 접속해서 여러 페이지를 방문하면 각 페이지마다 애플리케이션을 제공하는 구조입니다.

## 이슈

현재 겪고 있는 이슈는 PROBLEMS.md에 기록되어있습니다. 해당 파일은 Git에 커밋되지 않습니다.

워크트리 브랜치에서 작업하는 경우 PROBLEMS.md가 레포 최상위 디렉터리에 있다는 것을 인지하세요.

## 작업

### 서브에이전트

- "서브에이전트를 사용하라"는 지시가 없으면 서브에이전트를 사용하지 않습니다.
- 명시적인 지시가 있어도 서브에이전트를 무분별하게 사용하지 않습니다. 예를 들어 하나의 세션에서 구현 담당 서브에이전트, 리뷰 담당 서브에이전트를 호출하지 않습니다. **핵심은 한번에 서로 다른 역할의 서브에이전트를 호출하지 않는 것입니다.**
- 명시적인 최대 개수가 정해지지 않으면 최대 2개의 서브에이전트를 생성합니다.

### AI 에이전트 세션 활용

- 리서치 및 브레인스토밍
  - 주어진 주제에 대해서 자료를 수집할 때 서브에이전트를 사용해서 다양한 관점에서 검색 및 리서치를 진행할 수 있다. 이 때 서브에이전트를 리뷰나 구현에 사용하지 않습니다.
- 구현
  - 구현 작업이 주어지면 서브에이전트를 사용해서 구현 작업만 병렬로 진행할 것. 무조건 병렬로 진행하지 않습니다. 병렬로 진행할 수 있는 작업만 병렬로 진행할 것.
  - 즉 구현 작업 담당 서브에이전트와 리뷰 작업 담당 서브에이전트를 동시에 호출하지 않는다.
  - 워크트리 브랜치에서 작업하는 경우 레포 최상위 디렉터리에 `temps/reviews/<type>/<short-description>/<timestamp>-<name>-<nonce>.yaml` 파일을 확인해서 리뷰 사항을 주기적으로 확인할 수 있다.
- 리뷰
  - 서브에이전트를 사용해서 다양한 관점에서 현재 작업 사항을 리뷰할 수 있다.
  - 관련 워크트리 브랜치가 존재하는 경우 레포 최상위 디렉터리에 `temps/reviews/<type>/<short-description>/<timestamp>-<name>-<nonce>.yaml` 파일에 리뷰 결과를 작성한다.

### 구현 작업

- 구현 작업의 경우 모든 작업을 한번에 진행하지 않습니다. 작업을 모듈 단위로 단계적으로 분할한다.
- 각 모듈 작업마다 리뷰(검증, 검토 포함)를 최대 1회 진행한다.
- 무의미한 Fixture, 스크립트를 작성하지 않습니다.
- 불필요한 Fixture, 스크립트를 작성하지 않습니다.
- 기술스택에 어울리는 디자인 패턴을 사용합니다.
- 각 절차마다 무조건 코드를 추가하는 방식으로 작업하지 않습니다.
- 플랜을 세울 때 어떤 코드를 추가하고 제거할지 구체적으로 플랜을 구상합니다.
- 우리 기술스택과 유사한 기술스택으로 개발된 오픈소스 애플리케이션에 대해서 발생할 수 있는 안티패턴을 찾아서 우리가 무슨 코드를 작성하면 안되는지 파악합니다.

### 테스트

- 무의미한 테스트를 작성하지 않습니다.
- 모든 코드에 TDD를 적용하는 행위를 금지합니다. TDD가 필요한 기능(코드)일지 한번 더 확인합니다.
- 내부 구현을 검사하는 테스트 코드를 작성하지 않습니다.
- 하드코딩 기반 테스트 코드를 작성하지 않습니다.

### React Hooks

- React 기본 Hooks를 `.tsx` 파일에 작성하지 않습니다.
- UI와 Hooks의 관심사를 분리시키세요. Hooks 내부 구현이 바뀌어도 UI가 영향을 받지 않게 합니다.
- 다음과 같은 방식을 사용하지 않습니다.
  - Render Props
  - Children이 아닌 별도의 Props으로 JSX 전달
  - 컴포넌트가 아닌 일반 함수 호출로 JSX 렌더링
  - Props Drilling, Context API의 경우 반드시 필요한 상황에서만 적용합니다.
- 항상 메모리 사용, 렌더링 횟수를 절감할 수 있는 방법을 찾아서 적용합니다.
