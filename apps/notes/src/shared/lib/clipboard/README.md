# 클립보드 쓰기

이 모듈은 텍스트 쓰기 interface와 브라우저 Clipboard API 구현을 제공한다. 브라우저 구현은 쓰기 성공, API를 사용할 수 없는 상태, `NotAllowedError`와 그 밖의 쓰기 실패를 안정된 값으로 반환한다.

사용자 안내, 재시도 정책, 복사할 텍스트의 조합과 사용 횟수 기록은 이 모듈의 책임이 아니다. 각 사용자 동작이 실패 사유에 맞는 다음 행동과 저장 규칙을 정한다.
