# CSS class 이름 결합

이 모듈은 문자열, `false`, `null`과 `undefined`로 전달된 CSS class 이름 가운데 비어 있지 않은 문자열만 공백으로 연결한다.

조건을 선택하는 기준, Tailwind CSS class 충돌 해결과 구성요소의 시각 상태는 이 모듈의 책임이 아니다. 호출자는 동시에 적용하면 안 되는 class 이름을 전달하지 않는다.
